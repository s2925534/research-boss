# Changelog

All notable changes to Corroborly will be documented in this file.

## Unreleased

- Improved SerpApi's Google Scholar result mapping in `engine/scholar_providers.py`, per Pedro forwarding SerpApi's account-welcome email and asking me to use their docs/playground to check for missed metadata. Fetched `serpapi.com/google-scholar-api`'s documented request/response schema, then verified the actual shapes against 3 live requests on the real account (not guessed) before writing any parsing code. Found two real gaps: `venue` was always left `None` even though it's often present in `publication_info.summary` (format `"Authors - Venue, Year - Publisher"`), and the `resources[]` array's direct PDF links weren't captured at all. Added `_extract_venue_from_summary()` -- finds the " - "-delimited segment containing the year and strips it, returning `None` (not a guess) when nothing meaningful remains, e.g. summaries that are just `"Authors - Year - host.domain"` with no venue text at all, confirmed to occur in real responses. New additive `ScholarResult.pdf_url` field, populated from `resources[]` when a `file_format: "PDF"` entry exists. 3 new/updated tests using the real confirmed response shapes; verified live end-to-end via the actual CLI (not just crafted fixtures) -- got back a genuine result with `venue: "Global Transitions Proceedings"`, another correctly left `venue: null` (year+host only, no fabricated venue), and two real `pdf_url` links. Along the way, reconciled the local usage tracker for 4 completed round trips made directly (bypassing the CLI) while confirming the live schema, so the count stays honest. Full suite re-run clean (716 passed, 10 skipped). Bumped project version to 0.12.11.

- Added local SerpApi usage tracking to the Scholar fallback pipeline, per Pedro's explicit request after providing a real `SERPAPI_API_KEY` for his (free-tier, 250 searches/month) account. New `SerpApiUsage`/`read_serpapi_usage()`/`serpapi_usage_path()` in `engine/scholar_providers.py`: every completed round trip to serpapi.com (success or their own error payload; a transport-level failure that never reached them doesn't count) increments a local counter in `~/.corroborly/serpapi-usage.yaml`, deliberately account-wide rather than per-workspace since the quota is one shared resource. `_fetch_serpapi` now skips the request entirely once the tracked count reaches the cap, falling through to the next provider instead of risking an overage; `SERPAPI_MONTHLY_LIMIT` overrides the 250 default for a paid plan. Surfaced in two places: `search scholar`'s own attempt-detail line and a yellow/red console warning at >=80%/limit-reached, plus a new standalone `corroborly search scholar-usage` command to check anytime without running a search. Verified live end-to-end: added the real key to the local (gitignored) `.env`, ran a real `search scholar` query, got genuine SerpApi Google Scholar results back (e.g. the real Günther & Kim 2006 "Container terminals and terminal operations" paper), and confirmed the usage file correctly recorded `1/250`. Found and fixed a real test-isolation gap along the way: the new tests' `monkeypatch.delenv("SERPAPI_API_KEY", ...)` calls didn't actually simulate "key absent" once a real key existed in the repo-root `.env`, since `_env_value()` also reads that file directly regardless of `os.environ` -- added an autouse fixture stubbing the dotenv read in `tests/test_scholar_providers.py` so the suite stays hermetic regardless of what's in a developer's real `.env`. 11 new tests (8 provider-level, 3 CLI-level), full suite re-run clean (715 passed, 10 skipped) after the isolation fix. Bumped project version to 0.12.10.

- Resolved the app./folio. domain reconciliation flagged in the entry below: Pedro ran `synology-site cloudflare-route folio.corroborly.com --port 5111` himself (hit and fixed a PATH issue -- needed `.venv/bin/synology-site`), then explicitly authorized proceeding without him for the rest. `folio.corroborly.com/health` now returns a genuine `200`. `app.corroborly.com` is also still live on the same port -- whether to keep it as a permanent alias or remove its route is intentionally left undecided rather than guessed. `docs/DEPLOY.personal.md` (gitignored) updated to reflect the resolved state; next real step is registering the self-hosted CD runner. No code changed. Bumped project version to 0.12.9.

- Re-checked Phase 12 (Self-Hosted Deployment) live, per Pedro's "start Phase 12 with a real domain" request. Found it was more done than tracked: `app.corroborly.com/health` returns a real `200` today — re-verified live via `curl`, not assumed from an earlier session's notes — so "actually deploy somewhere reachable" and the README/LICENSE-vs-live-About-modal consistency check (checked directly against `LICENSE` and the README Author section — genuinely word-for-word matching) are both now `Done Done`. Also found a real, separate mismatch while re-checking: the live deployment is under `app.corroborly.com`, but `docs/DEPLOY.personal.md` and the tracked `.github/workflows/deploy.yml` have named `folio.corroborly.com` canonical since 2026-07-17, and that domain was never actually carried out (`curl` → `502`; `synology-site list --all-targets` shows only the `app-corroborly-com` target, no `folio` one). Asked Pedro which domain to finish under; he confirmed `folio.corroborly.com`. The one remaining step (`synology-site cloudflare-route folio.corroborly.com --port 5111`) has no `--dry-run` mode at all (confirmed via its own `--help`, not guessed) and is therefore unconditionally un-runnable by an agent regardless of how explicitly the target is named — reported the exact command in `docs/DEPLOY.personal.md` (gitignored) for Pedro to run himself rather than repeatedly attempting a call that will always be blocked. No code changed; TODO.md/README/DEPLOY.personal.md updated to reflect the real current state. Bumped project version to 0.12.8.

- Completed Phase 23 (Web UI — External Search and Abstract Screening), reprioritized and built per Pedro's explicit "prioritize phase 23 without my help" go-ahead. New "External search & abstract screening" web panel (`corroborly/web/templates/index.html`, `corroborly/web/static/app.js`): a deterministic query-plan generator, a candidate-reports refresh action (high-signal candidates table with per-row selection, plus a duplicates/Zotero-matches/evidence-gap/run-comparison summary), an "import selected as sources" action, and a legacy-abstracts-folder import trigger — all thin clients over the already-existing `POST /api/v1/search/plan`, `GET /api/v1/search/reports`, `POST /api/v1/search/import-candidates`, `POST /api/v1/abstracts/import` routes. Verified live end-to-end against a real running server with a seeded candidate (checked every rendered field — `high_signal.candidates`, `duplicates.duplicate_groups`, `zotero_matches.matches`, `evidence.candidates`, `comparison.runs` — against the real engine return shapes rather than guessed). Also added the AI-assisted query-planning/candidate-review web views ("External search: AI query plan" / "External search: AI candidate review") as two new options in the existing AI-assisted-features panel, now that the deterministic search UI they were explicitly deferred behind exists.
  While building and live-testing the AI views, found and fixed a real, separate pre-existing bug: five AI route handlers (`search_ai_query_plan`, `search_ai_candidate_review`, and `ai.py`'s `/review`, `/novelty`, and the shared `_workspace_report_route` factory behind six more routes) only wrapped credential *loading* in `try/except OpenAiError`, not the actual OpenAI request — a genuine call-time failure (network error, provider outage) crashed with an unhandled `500` instead of the documented error envelope. Reproduced live with a real `openai_post` call failing on this sandbox's TLS setup, then fixed all five sites with `try/except OpenAiError -> 502 ai_call_failed`, matching the pattern already used correctly elsewhere (`citations.py`'s AI-plan route, `ai.py`'s `review-document` route). 2 new regression tests forcing a real `URLError` through the real code path. Full suite green (705 passed, up from 703, 10 skipped).
  Also scoped (not built) two previously-vague TODO gaps into concrete, checkable sub-items per Pedro's request: Phase 27's missing paragraph/sentence derived-text-anchor mechanism (`engine.derived_text`, anchored `[[source:<id>#<anchor>]]` citation markers, an anchor-resolution API for Phase 31's per-suggestion popups) and Phase 32's "Trusted-pair LAN/SSH sync" brainstorm line (a proposed rsync-over-SSH design — existing `~/.ssh/config` aliases only, manifest-based conflict detection, explicit dry-run — flagged for Pedro's go-ahead, nothing built). Bumped project version to 0.12.7.

- Added a Google Scholar fallback data-provider layer, per an explicit request (there's no official Google Scholar API). New `corroborly/engine/scholar_providers.py` (`ScholarDataService`) is a standalone module -- it does not import from or get imported by `engine/external_search.py` (the existing Scopus provider), so it can't regress it. Sequential pipeline: SerpApi's Google Scholar engine (needs `SERPAPI_API_KEY`) -> Semantic Scholar's official Graph API (free, works without a key) -> the optional `scholarly` package (new `scholar` extra, lazy-imported so it's not a hard dependency) -> ScholarAPI.net. That last one ships as an honest stub that always raises, not a guessed request/response contract -- I could not verify its real endpoint, auth scheme, or response shape from documentation I trust, so rather than fabricate one it logs "not implemented" and falls through cleanly, same as a real outage would. Only exceptions trigger a fallback; a provider that legitimately returns zero results is treated as a valid answer, not a failure. Wired into `corroborly search scholar --external-search "query" [--max-results <n>]`, gated behind the same `--external-search` opt-in as Scopus and, matching Scopus, deliberately left CLI-only rather than also adding an `/api/v1` route, since both make live third-party network calls and the codebase's existing convention keeps that CLI-explicit rather than reachable from the web app. 17 new tests (14 provider-level with injectable HTTP openers/fake `scholarly` module, 3 CLI-level), full suite re-run clean (no regressions), verified live end-to-end through the real CLI command and confirmed the one real-network failure seen was this sandbox's own CA cert configuration (reproduced with a bare unrelated `urlopen` call), not a bug in the request code. Bumped project version to 0.12.6.

- Added self-service "change password" for the single shared login, per Pedro's explicit choice (asked between this and waiting for real per-account infrastructure). New `POST /api/v1/auth/change-password` rewrites `CORROBORLY_API_PASSWORD` in `.env`, requires the current password as defense in depth, and invalidates every existing session on success. Refuses with `409` rather than silently no-op'ing when credentials actually come from a real process env var (Docker/shell) instead of a live `.env` read -- honestly scoped as local CLI/dev-only for now; making it work for the deployed NAS container needs a separate docker-compose.yml change, not bundled here. New "Change password" modal in the web UI topbar. 3 new tests, verified live end-to-end. Bumped project version to 0.12.5.

- Extended the web UI's theme toggle to an explicit light/dark/system three-way picker (was a two-state light<->dark cycle button with no way back to "follow the OS"), matching `../Resilinked`'s pattern, per Pedro's 2026-07-17 request. New shared `corroborly/web/static/theme.js`, loaded by both the login page and the main app so the choice carries everywhere. Also added a show/hide password toggle and a "Remember me" 30-day-session checkbox to login -- the parts of Resilinked's login options that fit Corroborly's real single-shared-credential architecture; email/OTP/passkey/Google-OAuth were not built, since those need real per-account infrastructure (Phase 29, not started, gated on Pedro's own business decision) rather than a login-page skin. 1 new test, verified live via a real running server. Bumped project version to 0.12.4.

- Fixed a `docker-compose.yml` bug found by the first real live NAS deploy attempt: `postgres-backend`/`mariadb-backend` (both behind an inactive `profiles: ["db-backends"]`) used Compose's `:?required` interpolation syntax for their passwords, which Compose validates for every service at config-parse time regardless of profile activation -- so plain `docker compose up` (no `--profile db-backends`) failed outright on any host without `CORROBORLY_POSTGRES_PASSWORD`/`CORROBORLY_MARIADB_PASSWORD` set, even though neither service ever starts in that case. Changed both to `:-` (empty default), approved by Pedro 2026-07-17 after confirming the official postgres/mariadb images themselves still refuse to start with an empty password -- the "never silently run a DB with a weak/empty password" guarantee holds, just enforced at container-start time instead of compose-parse time, and only when the profile is actually invoked. Bumped project version to 0.12.3.

- Completed Phase 13's persistent navigation shell: the web UI's single page had grown to 22 stacked panels, genuinely crowded enough to revisit this deferred item. New `#app-shell` (sidebar `#app-nav` + `#app-content`) wraps all 22 existing panels (the "what changed" digest banner stays a global notification above the nav, not a page); `setupAppNav()`/`showNavSection()` in `app.js` build nav buttons from each panel's own `<h2>` text, toggle visibility via the `hidden` attribute (never removing panels from the DOM, so every existing element ID/listener keeps working unchanged), and persist the active section in `localStorage`. Pure frontend change, no new API surface. Responsive collapse to a horizontal strip under 860px. Full suite re-run clean (655 passed, 10 skipped); verified live via a real running server. Bumped project version to 0.12.2.

- Completed Phase 28's research-question wizard multi-step web UI flow. New `POST /api/v1/rqs/wizard/preview` (stateless candidate composition + readiness scoring, never saves) and `POST /api/v1/rqs/wizard/save` (saves one previewed candidate) -- no server-side wizard session, matching the API's existing stateless convention; the one-question-at-a-time UX is purely client-side step-through. New "Research question wizard" panel in the web UI's Research Questions section, mirroring `corroborly rqs wizard`'s exact 7-step prompt sequence and wording. 2 new tests, verified live end-to-end via a real running server. Bumped project version to 0.12.1.

- Renamed the project from Ledgerly to Corroborly, per Pedro's explicit 2026-07-17 go-ahead (`corroborly.com` now registered). Systematic rename across the whole repo, mirroring the earlier ResearchBoss → Ledgerly rename's playbook exactly: Python package `ledgerly/` → `corroborly/`, CLI command `ledgerly` → `corroborly`, every `LEDGERLY_*` env var → `CORROBORLY_*`, session cookie (`ledgerly_session` → `corroborly_session`), Docker service/container name, all docs/tests/web branding. 141 files, ~1650 occurrences swept; full test suite re-run clean after (653 passed, 10 skipped -- the skip increase is local Postgres/MariaDB test-server reachability, not rename-related). Also fixed two unrelated pre-existing bugs found while touching version strings: `corroborly/__init__.py`'s `__version__` had drifted out of sync with `pyproject.toml`'s version (the `version` CLI command reads the former), and README's "Current version" line was stale -- both now kept in sync at 0.12.0. GitHub repo renamed `s2925534/ledgerly` -> `s2925534/corroborly` via `gh repo rename`, local `origin` remote updated to match. Deploy target changed from `research.veloso.dev` to `app.corroborly.com` in the gitignored `docs/DEPLOY.personal.md` (root `corroborly.com` is reserved for a separate marketing/placeholder site, per `../corroborly`'s own README) -- `docker-compose.yml` also gained an explicit `container_name: corroborly-api` so the deploy tool's health-check verification doesn't depend on guessing Docker Compose's project-name-from-directory convention. Local project directory intentionally left as `ResearchBoss` on disk, matching the same call made during the prior rename (avoids disrupting this session's own working directory and any path-based tooling/hooks). Bumped project version to 0.12.0.

- Completed Phase 28's `paper draft --ai` and closed a real document-versioning gap found while building it, per Pedro's standing "do any remaining AI implementation" go-ahead. New `engine.artefact_creation.create_ai_paper_draft`: a thin orchestration layer over Phase 8's AI edit sessions (not a new drafting mechanism, per item 453's explicit instruction) that ensures the deterministic paper-draft skeleton exists first, then proposes replacing only its two known placeholder passages with grounded prose -- never the sources/claims tables. New `engine.artefacts.promote_ai_paper_draft`/`clear_paper_review_gate`: a mandatory review gate (`paper_review_gate: "requires_validate"`) that only clears after a genuine, file-mtime-fresh `corroborly validate` report exists for that exact artefact -- `set_artefact_review_status` itself now refuses `reviewed`/`accepted` transitions while the gate is open, so a paper can never silently become "final" just because AI produced it. Along the way, found that `create_deterministic_artefact` never snapshotted a document-vault version on write, and that `promote_ai_paper_draft` overwriting the original artefact path wasn't reflected in that path's own version history (only the `.ai-edited.md` side file's chain showed it) -- fixed both, so every artefact this project produces is now versioned from creation through every AI promotion, restorable via the existing `doc version/versions/diff/restore/compare` mechanism (Phase 8, already built) with zero new UI needed. `corroborly paper draft <rq-id> --ai --full-target-document-ai`, `paper promote-ai-draft <rq-id> <session-id>`, `paper clear-review-gate <rq-id>`; `POST /api/v1/artefacts/paper-draft/ai`, `.../promote`, `.../clear-review-gate`; web UI "AI-assisted paper draft (optional)" panel. 9 new tests (662 total, 1 skip), verified live end-to-end: real CLI reached the actual OpenAI network boundary, and a full fake-opener run confirmed anchor-verified edit -> accept -> apply -> promote -> gate-blocks-reviewed -> gate-refuses-without-validate -> real `validate_document` run -> gate-clears, with the original artefact path's version history showing every step. Bumped project version to 0.11.59.

- Added optional AI-assisted cross-reference suggestions (Phase 22/8 item), per Pedro's explicit 2026-07-17 go-ahead. New `engine.cross_reference.ai_cross_reference_suggestions`, additive to the existing deterministic keyword-overlap pass -- appends AI-suggested candidates (`match_basis: "ai_suggested"`, with a `rationale`) to the same candidates report, never replacing it. Every proposed target is validated against the real workspace; a hallucinated ID is silently dropped, never trusted. Never sends the uploaded file's own content, only safe context plus the upload's title/filename. `apply_cross_reference_links` needed zero changes -- AI candidates flow through the same review-then-apply pipeline. `corroborly doc cross-reference-ai <upload_id> --ai`, `POST /api/v1/artefacts/cross-reference/ai`, and a "Suggest more with AI" button in the existing cross-reference review modal. 6 new tests (654 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.58.

- Completed the "AI-assisted citation planning as a web view" item (Phase 22/23): new `POST /api/v1/citations/ai-plan` (no route existed for `cite ai-plan`'s AI tier at all before tonight), same double `ai`/`full_target_document_ai` opt-in as the AI-edit-sessions route, replicating the CLI's exact deterministic-plan-then-AI-layer merge behavior. New "AI-assisted plan review" collapsible section in the existing web Citation Planning panel (not a new panel), with its own consent checkbox and grounding badge. 2 new tests (648 total, 1 skip), verified live via the real running server. Bumped project version to 0.11.57.

- Completed Phase 25's `ai review-document` command, per Pedro's explicit 2026-07-17 go-ahead. New `corroborly.engine.ai.ai_review_document`: sends the target's full text (requires `--full-target-document-ai`, the same double opt-in as `cite ai-plan`) plus accepted-source safe context, the claim ledger, the target's own citation plan if one exists, and only explicitly opted-in note kinds (`note_kinds` -- a per-kind toggle, not one blanket switch, mirroring `ai context-preview`'s boundary-drawing precedent, so a user can include source text and claims without exposing personal meeting notes). Produces a structured Strengths/Weaknesses/Unsupported-Claims/Suggested-Revisions report, grounding-checked against sources/claims/notes together; never modifies the target. Made `engine.citations._plan_path` public as `citation_plan_path` since this feature needed it cross-module. `corroborly ai review-document --ai --full-target-document-ai [--include-notes] [--include-meeting-notes] [--include-transcripts]`, `POST /api/v1/ai/review-document`, and folded into the existing web AI-assisted-features dropdown (not a new panel) as a "Review a working document" option. 8 new tests (646 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.56.

- Completed Phase 8's structured AI edit sessions, per Pedro's explicit 2026-07-17 go-ahead to implement all remaining AI-tagged work autonomously -- the propose-then-apply foundation Phases 22/25/28/31 all reference as their shared mechanism. New `corroborly.engine.ai_edit_sessions` (`create_ai_edit_session`/`set_ai_edit_review_status`/`apply_ai_edit_session`): proposes AI edits to a Markdown target anchored to specific paragraph/sentence IDs from a fresh derived-text snapshot, using a deterministically parseable `### EDIT paragraph_id=... sentence_id=...` block format (mirroring Phase 27's citation-marker convention). Every proposed edit's claimed anchor is verified against the real document (`anchor_verified: false`, never silently trusted, if the model's claimed original text doesn't actually match) and the whole response is grounding-checked. Requires the same `--full-target-document-ai` double opt-in as `cite ai-plan`. Applying only acts on `accepted`/`approved` edits, writes a new document version (never modifies the target in place), and wraps each applied replacement in a new `[[AI-EDIT-START]]...[[AI-EDIT-END]]` marker (`engine.grounding.wrap_ai_edit_span`) so AI text stays visibly distinguishable directly in the raw file per AGENTS.md Core Rule item 4. Scoped to `.md` targets only (derived-text anchors only exist for Markdown). `doc ai-edit-session-create/list/review/apply`, `POST/GET /api/v1/doc/ai-edit-sessions*`, and a new web panel in the Document Vault section. 14 new tests (638 total, 1 skip), verified live end-to-end via the real CLI (create → review → apply, confirming the pre-edit snapshot, derived-text anchors, and the real OpenAI network call were all reached correctly before an environment-specific TLS failure -- no mocked shortcut past that boundary). Bumped project version to 0.11.55.

- Added a proactive "what changed since you were last here" digest (Phase 32 brainstorm item, closing out the phase): new `corroborly.engine.digest` (`since_last_visit_digest`, `mark_visited`/`last_visited_at` in `app-settings.local.yaml`), built entirely from data this project already timestamps -- the claim ledger's `created_at`/`updated_at` and the existing `timeline_report` (run summaries, decisions, terminology, feedback, context changes) -- plus the existing stale-open-claims count. Honestly scoped: checked, not assumed, that sources and personal notes have no timestamp field anywhere in this codebase, so new-source/new-note activity isn't reflected here, a real separate gap not silently claimed as covered. `corroborly digest [--no-mark-visited]`, `GET /api/v1/reports/digest?mark_seen=`, and a new banner at the top of the web UI's main view shown automatically on workspace load, marking the workspace visited as a side effect. 11 new tests (624 total, 1 skip), verified live end-to-end via the real CLI (a genuine first-visit → add-a-claim → second-visit round trip). Bumped project version to 0.11.54.

- Added reusable workspace templates (Phase 32 brainstorm item): `corroborly templates save/list` and `init --template <name>`. New `corroborly.engine.templates` module -- templates live outside any single workspace (`CORROBORLY_TEMPLATES_ROOT`, default `~/.corroborly/templates`, mirroring `CORROBORLY_WORKSPACE_ROOT`'s override pattern), capturing project type/citation style/primary output type/source-review-default/upload-prevention plus the *actual* guideline files (re-registered through the normal `register_guideline` path on apply, not just copied) and which were marked default. `init --template` skips (with the value printed, not silently) every setup question the template supplies while leaving the rest of the interactive wizard unchanged. AI preference is deliberately never templated -- always asked fresh, since an AI opt-in stance is a per-workspace decision (AGENTS.md Core Rule), not something a saved template should carry forward. CLI-only, no API/web surface (workspace creation has no API/web equivalent today either). 11 new tests, verified live end-to-end via the real CLI (`templates save` → `templates list` → `init --template`, confirmed pre-filled setup values and a genuinely copied, re-registered guideline). Bumped project version to 0.11.53.

- Added a visual green/yellow/red grounding confidence badge to the web UI (Phase 32 brainstorm item), built on tonight's Phase 27 `grounding` field: new `groundingBadgeHtml()` in the AI-assisted-features panel's result view (reusing the existing `.candidate-status` badge CSS) and a simplified badge variant in the AI usage log table, replacing/augmenting the plain text warning. Pure presentation layer over data the API already returns — no engine/API changes, no new tests (verified with `node --check` and a live running server render). Bumped project version to 0.11.52.

- Added optional stage target dates plus a deterministic `.ics` calendar export (Phase 32 brainstorm item): checked, not assumed, that there was no stage read/update mechanism at all before this (`research_stage_template` only ever wrote a static default at `init`/migration time) — closed that gap first. New `corroborly.engine.research_stages` (`list_stages`, `set_stage_status`, `set_stage_target_date`, `stages_ics`/`write_stages_ics`, hand-written RFC 5545 generation, stdlib only); stages without a target date are simply omitted from the calendar. `corroborly stages list/status/target-date/ics`, `GET/POST /api/v1/stages*`, and a new "Research stages" web panel. 15 new tests (602 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.51.

- Added optional encrypted local backups (Phase 32 brainstorm item, mechanism confirmed by Pedro 2026-07-17): `corroborly backup --encrypt` / `corroborly backup-decrypt`. Real discovery mid-build changed the planned mechanism: `age -p`'s passphrase mode was verified live to hard-require an interactive `/dev/tty` prompt with no non-interactive path at all, so it can't back a scriptable flag; switched to `gpg --batch --passphrase-fd 0 --symmetric`, verified live with a genuine non-interactive encrypt/decrypt round trip. New `engine.backup.create_encrypted_workspace_backup`/`decrypt_workspace_backup` (subprocess, detect-don't-bundle, matching the tesseract/SourceScribe pattern); the passphrase is piped via stdin, never argv or an env var, and the plaintext zip is deleted immediately after encryption (even on failure). CLI-only for now — accepting a backup passphrase over HTTP is a materially different security surface than a local terminal prompt, not silently added to the API. 8 new tests, including a genuine (non-mocked) `gpg`-backed round trip skipped cleanly where `gpg` isn't installed, verified live end-to-end via the real CLI. Bumped project version to 0.11.50.

- Added a self-contained HTML viewer for the supervisor export bundle (Phase 26 brainstorm item): new `engine.export.build_supervisor_bundle_html` writes `supervisor-bundle.html` (inline `<style>` only, no external assets or JS, all user content HTML-escaped) with the same claim ledger, citation plans, workspace review report, and AI usage disclosure as the existing Markdown digest — openable by double-click with no Corroborly install or Markdown renderer needed. Both formats now share one `_gather_supervisor_bundle_data` helper so they can't silently drift apart. `build_supervisor_bundle`'s zip always includes the HTML file too, no separate call needed. 2 new tests (579 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.49.

- Added deterministic (non-AI) near-duplicate claim detection (Phase 32 brainstorm item): `engine.claims.find_duplicate_claims`/`write_duplicate_claims_report` use stdlib `difflib.SequenceMatcher` pairwise text similarity (default threshold 0.85, no new dependency) to flag likely duplicate claim pairs for human merge/dismiss review — hygiene over a long project where the same finding can get logged twice under slightly different wording. Never merges or changes anything, only flags. Considered reusing the Phase 7 FTS5 index instead (the item's own suggestion) but FTS5 is built for keyword retrieval across many documents, not pairwise near-duplicate scoring, so plain stdlib similarity was the better fit. `corroborly claims duplicates --threshold`, `GET /api/v1/claims/duplicates` (`400 invalid_duplicate_threshold` outside (0, 1]), and a "Find likely duplicates" button in the web claims panel. 8 new tests (577 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.48.

- Added an auto-generated "how AI was used" disclosure section to the supervisor export bundle (Phase 32 brainstorm item), built on the AI-usage audit ledger added earlier tonight: `engine.export.build_supervisor_bundle` now has a "## AI Usage Disclosure" section (total AI calls, how many actually used a provider vs. correctly refused with insufficient evidence, grounding pass/fail counts, and a per-call table), reading only already-recorded metadata — never a new AI call. The raw `ai-usage-ledger.yaml` is now also included in the bundle zip alongside the existing claims YAML. Increasingly expected by journals/institutions requiring AI-assistance transparency in submitted work. 1 new test (569 total, 1 skip), verified live end-to-end via the real CLI (`corroborly export-supervisor-bundle`). Bumped project version to 0.11.47.

- Added a single AI-usage audit ledger (Phase 32 brainstorm item): `engine.ai._record_ai_usage` now records every call any `engine.ai` function makes to `ai-usage-ledger.yaml` (`{id, timestamp, kind, ai_used, insufficient_evidence, model, response_id, requires_user_review, grounding_fully_grounded}`), including calls that correctly refused via `insufficient_evidence` -- previously only novelty assessments got a persisted ledger entry, so there was no single place to answer "when was AI used on this workspace, and was it grounded." `list_ai_usage(workspace)` reads it back, tolerant of a missing file (unlike this project's other ledgers) since it now runs unconditionally on every AI call — a pre-upgrade workspace's first AI call afterward must not crash. Reachable via `corroborly ai usage-log`, `GET /api/v1/ai/usage-log` (no `ai: true` opt-in needed, read-only), and a new "AI usage log" panel in the web AI-assisted-features section. 7 new tests (568 total, 1 skip), verified live end-to-end via the real CLI. Bumped project version to 0.11.46.

- Added citation style selection (Phase 32 brainstorm item): `corroborly.engine.references` gained `format_reference`/`format_inline_citation` dispatching to MLA/Chicago/IEEE formatters alongside the existing APA7 one (`CITATION_STYLES`), explicitly labelled as simplified, common approximations rather than full style-guide-compliant implementations — a real gap, checked (not assumed) against `engine.citations._inline_citation`, which was hardcoded to one author-year style with no way to select another. Threaded through `create_citation_plan(..., citation_style: str = "apa7")` (new `citation_style` plan field, restyled `references` list, an extra `limitations` line for non-`apa7` styles), `corroborly cite plan/ai-plan --citation-style`, `POST /api/v1/citations/plan` (`400 invalid_citation_style` for an unrecognized value), and a new style dropdown in the web citation-planning panel. `apa7` output is byte-identical to before this field existed — verified via the full existing test suite passing unchanged. `ieee` assigns running reference numbers by order of first appearance, stable per plan generation. Scoped to citation-plan insertions/references only, not BibTeX export (a separate, already-standardized format with its own fixed spec). 24 new tests (561 total, 1 skip), verified live end-to-end via the real CLI (`corroborly cite plan --citation-style ieee` against a real workspace). Bumped project version to 0.11.45.

- Added `docs/FLAGS.md` (Phase 32 brainstorm item), a single index of every explicit opt-in flag and environment variable this project has accumulated phase by phase (`--ai`, `--external-search`, `--full-file-ai`, `--directory-ai`, `--full-target-document-ai`, `--full-source-document-ai`, `--allow-candidate-citations`, `CORROBORLY_DB_BACKEND`/`CORROBORLY_POSTGRES_*`/`CORROBORLY_MARIADB_*`, `CORROBORLY_SOURCESCRIBE_PATH`, etc.) with what each unlocks and its privacy/cost implication, plus an explicit "what is never a flag" section for the architectural guarantees (Zotero no-write boundary, no whole-document AI uploads by default) that have no override at all. A documentation-only addition — every entry cross-checked against the real flag/env-var name in `corroborly/cli.py`, `corroborly/engine/ai.py`, and `.env.example` rather than written from memory. Bumped project version to 0.11.44.

- Completed the remaining deterministic items of Phase 27 (AI Output Grounding and Anti-Hallucination Enforcement), the foundational mechanism the "No Hallucinations" Core Rule needed. New `corroborly/engine/grounding.py` (pure text/dict processing, never calls an AI provider): defines an inline citation marker convention (`[[source:<id>]]`/`[[claim:<id>]]`/`[[artefact:<id>]]`/`[[note:<id>]]`) that `citation_instruction()` appends to every grounded AI prompt, and `validate_grounding()` deterministically checks every marker found in a response against the real IDs actually present in that request's safe context/payload/citation plan — returning `fully_grounded`, `ungrounded_citations` (catches a model inventing a source/claim/artefact/note ID), and `uncited_paragraph_count` (a softer, unverifiable-content coverage signal). Wired into all 5 `engine.ai` functions (`ai_assisted_review`, `ai_novelty_assessment`, `ai_research_question_assessment`, all 8 `ai_workspace_report` kinds, `ai_citation_plan_review`) as a new `grounding` response-envelope field (`null` only when `insufficient_evidence`), automatically inherited by every API route since they're thin pass-throughs, and surfaced as a visible warning in the CLI's `_print_ai_review_footer` and `cite ai-plan` whenever a response isn't fully grounded or has uncited content. Also added `wrap_ai_generated_text()`/`strip_ai_provenance_markers()` — a reusable primitive that visually/structurally distinguishes AI-generated text from user-authored text wherever it's later inserted into a shared document, honestly scoped as not yet wired into a live feature since nothing inserts AI text into a mixed document yet (Phase 8's AI edit sessions, Phase 28's `paper draft --ai`, and Phase 31 are all still pending — this is the primitive they should share). New `tests/test_ai_grounding_adversarial.py` (Phase 27's required adversarial regression suite): proves every one of the 5 AI functions (all 8 workspace-report kinds parametrized) refuses via `insufficient_evidence` on a zero-evidence workspace, and separately proves the grounding-check catches a fabricated citation via a mocked AI response — honestly scoped: today's evidence check is structural (any evidence present), not semantic (evidence relevant to the query), so an off-topic-but-nonempty workspace still gets an AI call; the grounding-check is what proves that case is *flagged* rather than silently trusted. Also wired a matching warning banner into the web UI's existing AI-assisted-features panel (`runAiAction` in `app.js`) — the `grounding` field rides along on every existing `/api/v1/ai/*` route for free since they're pass-throughs, so this needed no API changes, only rendering the field that was already there (verified with `node --check` and a live running server). Extended `docs/api/CONTRACT.md`'s AI Routes section with the concrete `grounding` field shape and a worked example. Verified live via the real CLI (`corroborly ai review --ai` against a real, freshly-initialized empty workspace with a dummy API key — confirmed no network call, correct "Insufficient evidence" footer, and `grounding: null` in the written YAML); the "has evidence, response gets checked" path is verified through the mocked pytest suite only, not a real paid OpenAI call, matching this project's existing practice. 37 new tests (537 total, 1 skip). Bumped project version to 0.11.43.

- Completed Phase 30 (Audio/Video Transcription via SourceScribe), per Pedro's explicit 2026-07-16 go-ahead on the subprocess integration mechanism. New `corroborly/engine/transcription.py` shells out to a sibling SourceScribe checkout's `main.py` (`CORROBORLY_SOURCESCRIBE_PATH`, never imported — same decoupled-subprocess pattern this project already uses for `tesseract`/`pdftoppm` OCR) with a fresh job-scoped `--source-dir` scratch directory per run, so `completed/*/report.json` is unambiguous to glob; on success, the transcript is imported into Phase 25's notes store via the existing `import_transcript` (no AI processing on the transcript text itself). New workspace file `transcription-jobs.yaml` tracks each job's `pending`/`transcribing`/`completed`/`failed` status. Synchronous only, per this phase's own explicit framing — no background-job infrastructure added. `use_ai` (SourceScribe's own optional OpenAI backend) defaults to `False` everywhere and is never a silent fallback from local Whisper, matching AGENTS.md's Core Rule: No Hallucinations. New CLI group `corroborly transcribe {readiness,upload,start,status,list}`; new `corroborly/api/routers/transcription.py` mounted at `/api/v1/transcription` (`readiness`, `upload`, `jobs`, `jobs/{id}`, `jobs/{id}/start`); new web "Audio/video transcription (SourceScribe)" panel (upload form, start-job form, jobs table) below Personal Notes. Documented supported formats/size limits/deployment prerequisites in `.env.example`, `Dockerfile` (explicit non-bundling call-out — SourceScribe is a whole separate project, not an apt package, unlike tesseract/poppler), and a new section in `docs/PACKAGING.md`. Verified live end-to-end against the real sibling `transcriber` project, not mocked: a genuine local-Whisper transcription run through the actual CLI, the actual API (via `TestClient` and a real running `uvicorn` server), and the web page's rendered HTML, confirming the resulting note actually lands in `personal-notes.yaml`. 26 new tests (499 total, 1 skip), including genuine (non-mocked, `skipif`-gated) end-to-end runs against the real local SourceScribe checkout at both the engine and API layers, alongside unit-level coverage (extension/size validation, unknown-job/wrong-status error paths, unconfigured-SourceScribe error paths) that runs unconditionally in CI where no sibling checkout exists. Bumped project version to 0.11.42.

- Completed Phase 24 (Multi-Database Backend), per Pedro's explicit 2026-07-16 go-ahead. Optional MariaDB/PostgreSQL backend that mirrors the SQLite cache; SQLite itself stays permanently mandatory and zero-config — scoped away from the phase's original "optionally deactivate SQLite" idea, since every existing `engine.database` function (including FTS5 search) assumes SQLite exists, making that a materially larger, separate architectural change than "add a mirror." New `corroborly/engine/db_backends/` package (`base.py` for driver-agnostic mirror/repopulate row-copy logic, `postgres.py`/`mariadb.py` for connection + schema DDL, `config.py` for env var resolution); `psycopg2-binary`/`PyMySQL` are optional extras, only imported when actually configured. `CORROBORLY_DB_BACKEND` (+ per-backend host/port/user/password/database vars) never activates anything automatically — `db init`/`db sync`/`db status` (CLI, skipped entirely under `--quiet`) and a new web "Secondary database backend" panel only ever prompt. `POST /api/v1/db/{activate-backend,deactivate-backend,repair-sqlite,repair-backend}` and `GET /api/v1/db/backend-status` (documented in `docs/api/CONTRACT.md`); `db sync`/`db rebuild` mirror to an active backend automatically as their last step. Enforces at most one secondary backend active at a time. Optional (non-default) `docker compose --profile db-backends` profile adds local MariaDB/PostgreSQL containers; `.env.example` documents both var sets. Updated AGENTS.md's Privacy Rules to state the always-on-SQLite/opt-in-secondary guarantee explicitly. Caught and fixed a real bug while verifying live against a real PostgreSQL server: the "active backend" marker lived only in SQLite's own `meta` table — exactly the file missing in the "SQLite was deleted, repair from the secondary" scenario — moved to a workspace-root YAML file that survives SQLite's absence. Verified for real: started a local PostgreSQL 14 server and ran the full activate/sync/status/deactivate/both-repair-directions/at-most-one-active flow through the engine, CLI, and API layers with genuine network round trips, not mocks; MariaDB has no local server in this environment, so those tests are written identically but skip visibly rather than being silently omitted. 22 new tests (473 total, 1 skip). Bumped project version to 0.11.41.

- Completed Phase 22 (Web UI — AI-Assisted Features), per Pedro's explicit 2026-07-16 go-ahead resolving the AI opt-in/cost decision this phase was blocked on. Built the full "Future AI Routes" sketch in `docs/api/CONTRACT.md` for real: `POST /api/v1/ai/{test,review,novelty,rqs/assess,corpus-summary,claim-check,citation-gaps,artefact-cross-reference,source-relevance,abstract-screening}` (new `corroborly/api/routers/ai.py`, thin pass-throughs to the existing `engine.ai` functions, no new business logic) plus `POST /api/v1/search/{ai-query-plan,ai-candidate-review}` (added to `corroborly/api/routers/search.py`, each requiring both `ai: true` and `external_search: true`, matching the CLI's double opt-in). Every route requires the per-request `"ai": true` opt-in with no session/workspace-level bypass, resolves the OpenAI key server-side only (never from the request body), and inherits the `insufficient_evidence` short-circuit built earlier — never calls OpenAI when there's no real evidence to ground a response in. New "AI-assisted features" web panel (single action dropdown covering all 9 review/summary/assessment actions, a consent checkbox that un-checks itself after every run so each invocation is a deliberate re-opt-in, and an AI-readiness check button) — the web UI's first AI feature of any kind. Updated `docs/api/CONTRACT.md`'s "Future AI Routes" section (renamed to "AI Routes") to describe these as implemented rather than sketched, and TODO.md's Phase 22 header to explain why this doesn't skip ahead of Phase 27: these are read-only advisory reports never inserted into any document/artefact, so Phase 27's provenance-tagging requirement (specifically about AI text inserted somewhere a human might mistake it for their own) doesn't apply — only the insufficient-evidence requirement does, and that's done. 17 new tests (452 total), verified live end-to-end (template render, and a full mocked-OpenAI round trip through the actual API and engine layers). Bumped project version to 0.11.40.

- Completed Phase 28's deterministic paper-skeleton half: `corroborly paper draft <rq-id>` (new `paper_app` CLI group) assembles a deterministic, AI-free paper skeleton — hypothesis statement, background/literature review from accepted sources, evidence assembled from claims linked to that specific research question, and an explicitly unfinished conclusion placeholder. New `paper-draft` artefact type in `SUPPORTED_ARTEFACT_TYPES` (`artefacts/papers/paper-draft-{rq_id}.md`, always rq-scoped, required not optional). Deliberately never auto-classifies a claim as supporting or refuting the hypothesis — that's a judgment call, not a deterministic fact extraction, so it stays a reviewable placeholder rather than a guess presented as done, consistent with AGENTS.md's Core Rule: No Hallucinations. Free via the API through the existing generic `POST /api/v1/artefacts/create` route; added the missing `rq_id` field to the web UI's artefact-create form (previously had no way to specify one at all, even for `research-question-brief`'s optional filter). 7 new tests, verified live end-to-end via the real CLI (wizard → linked claim → paper draft → `corroborly validate`, plus overwrite-protection and unknown-RQ error paths). Bumped project version to 0.11.39.

- Started Phase 28 (the stated core project goal: topic → refined, falsifiable RQ → draft, AI-gated behind deterministic review). Shipped the fully deterministic RQ half: `corroborly rqs wizard` (a new standalone entry point, usable any time — not just during `init`) walks through scope/context, the phenomenon or relationship of interest, question type (descriptive/comparative/causal/evaluative — a fixed research-methods taxonomy), the assumption being tested, and support/refutation criteria, then deterministically composes one or more research questions (`compose_research_question`, a type-specific stem) — splitting a multi-angle answer into separate candidates (`split_candidate_relations`) the user keeps or discards individually, each shown its deterministic readiness score before saving. New `corroborly.engine.research_questions.add_research_question_candidate` closes a real pre-existing gap: there was previously no way to add a draft RQ outside of `init_workspace` at all. Research question data model gained additive `hypothesis`/`question_type`/`proof_criteria`/`disproof_criteria` fields — existing RQs and workflows (`rqs list/approve/reject/archive/check/assess`) keep working unchanged without them. 8 new tests, verified live end-to-end via the real CLI with scripted input (3-angle split, 2 kept/1 discarded, then ran `rqs approve` on the result to confirm downstream commands work unchanged). Bumped project version to 0.11.38.

- Completed Phase 27's insufficient-evidence response mode across every `engine.ai` function. Scoped honestly to what's deterministic: a hard pre-flight guard skips the OpenAI call entirely — no network request, verified with tests whose fake `opener` raises if invoked — whenever the safe context has zero usable source excerpts, or (for `ai_workspace_report`'s 8 report kinds) whenever the entire payload is empty, not just source excerpts, since abstract-screening/query-generation kinds legitimately draw on candidate data instead. Returns `insufficient_evidence: true` and a plain-language reason immediately, per AGENTS.md's Core Rule item 3 ("insufficient evidence" is a required, valid, successful output, not something to route around). Fixed 4 CLI call sites that were about to print the misleading "Human review is required before using this output" for a response where nothing was actually generated — new `_print_ai_review_footer` distinguishes the two outcomes correctly. Updated `docs/api/CONTRACT.md`'s Future AI Routes section to describe this as implemented rather than sketched. The harder case — the model itself deciding mid-answer that one specific claim isn't grounded — needs a structured/JSON-schema AI response redesign and stays explicitly open, not silently claimed as covered. 5 new tests (424 total), verified live end-to-end via the real CLI (`corroborly ai review --ai` against an empty workspace with a dummy API key, confirming no network call was attempted). Bumped project version to 0.11.37.
- Added CI, which didn't exist at all before this (Phase 27): `.github/workflows/tests.yml` runs the full pytest suite on every push/PR to main. New `tests/test_zero_ai_smoke.py` proves "the tool fully works with zero AI configured" (AGENTS.md Core Rule item 1) is an enforced guarantee, not an assumption — explicitly unsets `OPENAI_API_KEY`/`SCOPUS_API_KEY` and runs a real smoke pass (`doctor`, `version`, `status`, and the `list`/`timeline`/`research-progress`/`citation-relationships` commands) against a freshly-initialized workspace. Verified before writing the workflow, not assumed, that no existing test needs real network access — every AI-touching test already mocks its own network call. 1 new test (420 total). Bumped project version to 0.11.36.
- Completed Phase 27's FTS5-relevance half of "AI context must be assembled from indexed/chunked excerpts, not whole-file dumps" (AGENTS.md Core Rule item 6, added 2026-07-16). `engine.ai.build_safe_context` gained an optional `query` param; new `corroborly.engine.database.relevant_source_excerpts` ranks and excerpts accepted sources by FTS5/`bm25()` relevance to that query instead of truncating each source from character 0. All four callers (`ai_assisted_review`, `ai_novelty_assessment`, `ai_research_question_assessment`, `ai_workspace_report`) now supply a query — the workspace's stated topic, or joined research-question text where that's more specific (novelty/RQ assessment). Falls back to the original from-the-start truncation per source whenever no query is given, no SQLite index exists (`db sync` never runs automatically), or a given source has no FTS match — the zero-SQLite, zero-AI-configured guarantee (Core Rule item 1) holds either way, verified with a dedicated fallback test. The paragraph/sentence derived-text-anchor half of this Core Rule item (chunk-level citation precision within an already-selected excerpt) is explicitly *not* done here — tracked separately for Phase 31's per-suggestion source popups, not silently folded into this pass. Updated AGENTS.md's Core Rule item 6 to describe current behavior instead of the prior aspirational framing. 4 new tests (419 total), verified live end-to-end: captured the actual OpenAI request payload `ai_assisted_review` sends against a real synced workspace and confirmed it carries the query-relevant excerpt, not the from-start one. Bumped project version to 0.11.35.
- Closed Phase 23's deterministic backend-parity gap: web API routes for external-search query planning, report regeneration, and reviewed-candidate import (all previously CLI-only). New `POST /api/v1/search/plan`, `GET /api/v1/search/reports`, `POST /api/v1/search/import-candidates` (`corroborly/api/routers/search.py`) and `POST /api/v1/abstracts/import` (`corroborly/api/routers/abstracts.py`), both mounted in `corroborly/api/app.py` and documented in `docs/api/CONTRACT.md`. All four are pure passthroughs to existing engine functions — no new business logic, no external API calls (Scopus access stays CLI-only behind `--external-search`), matching this phase's "deterministic parts only" scope; AI-assisted query planning/candidate review and abstract-screening remain explicitly blocked on the AI opt-in decision, unchanged. No web UI views added yet (out of scope for this pass — Phase 23 is explicitly lower-priority/blocked). 7 new tests (417 total), verified live end-to-end (real workspace query plan generation, report regeneration, candidate import, and a real legacy-abstract-file import). Bumped project version to 0.11.34.

- Closed two stale TODO items found while auditing what's left after Phase 26: their engine functions, CLI commands, and API routes already existed (source review report `GET /api/v1/sources/report`, Phase 14; derived-text/anchor builder `POST /api/v1/doc/derive-text/{version_id}`, Phase 18) but neither had a web UI trigger and neither checkbox had been updated. Added a "Review report" button to the Sources panel (not-yet-converted/untagged/without-notes summary counts) and a "Build anchors" action to the Document Vault panel (paragraph-count confirmation) — the anchor builder stays a single confirmation line rather than a full per-anchor viewer, matching that item's own "prep infrastructure, not a standalone view yet" framing. No Python changes — verified with the full test suite (412 passing, both routes already had API-level tests), a live end-to-end smoke test (real document snapshot → derive-text → paragraph count), and a Node syntax check. Bumped project version to 0.11.33.
- Completed the final Phase 26 brainstorm item: a document gallery view, as a grid alternative to the Uploaded Artefacts flat list. Deliberately scoped to uploaded artefacts rather than the Sources list: uploaded artefacts already have a secure preview route confined to `document_vault/` (`GET /api/v1/artefacts/uploads/{upload_id}/file`), while a Source's `file_path` can point anywhere on disk — any folder the user scanned, not necessarily inside the workspace — so building raw file-serving for arbitrary source paths would be a materially riskier surface than reusing what already exists safely; that's a separate decision, not something to fold in silently. Image files render a real `<img>` thumbnail (browser-scaled, no new dependency); everything else gets a type icon by extension (no PDF-thumbnail-rendering dependency added). List/Gallery toggle buttons; clicking a tile reuses the exact same `openPreview()` modal the list view's "Preview" button already used. Keyboard-accessible (`tabindex`, Enter/Space activate a tile). No Python changes — verified with the full test suite (412 passing, unaffected), a live end-to-end upload→list→file-serve API smoke test, and a Node syntax check. Bumped project version to 0.11.32. This closes out every item in Phase 26.
- Completed the last two Phase 26 brainstorm items: full-text corpus search. Phase 7 built a SQLite FTS5 index (`fts_index_search`) but nothing anywhere ever queried it — this wires it up. New `corroborly.engine.database.search_corpus` runs an FTS5 `MATCH`/`bm25()` query and returns ranked results with highlighted snippets; never auto-creates or activates the SQLite index (`status: "not_indexed"` with a `db sync` hint if it doesn't exist yet), matching AGENTS.md's opt-in-cache rule. Found and fixed two real gaps while building this: (1) personal notes/meeting notes/transcripts were entirely missing from the indexed document set despite the TODO item explicitly naming "notes" as one of the four things to cover — added `personal-notes.yaml` to `_fts_documents`; (2) raw FTS5 `MATCH` syntax chokes on ordinary English words containing `-` or `:` (e.g. "self-driving" errors as invalid syntax) — added `_fts_query_terms`, which quotes each word as a literal phrase token so a plain keyword search box never surfaces a raw SQLite syntax error for common punctuation. New `corroborly search-corpus <query>` CLI command, `GET /api/v1/db/search?query=...` API route (documented in `docs/api/CONTRACT.md`), and a "Search corpus" web panel with a `/` keyboard shortcut to jump to it (added now that the search box it was deferred for actually exists) — fixed a real XSS risk caught before shipping: the SQLite `snippet()` highlight markers were being inserted as raw HTML, which would have let corpus content containing `<script>` execute; fixed by escaping the snippet text first and only then converting the still-intact `[`/`]` markers to `<mark>` tags. 6 new tests (database + API, 412 total), verified live (CLI end-to-end including the hyphenated-word case, a login+render+search API smoke test). Bumped project version to 0.11.31.
- Completed another Phase 26 brainstorm item: confirm dialogs for destructive web UI actions. Checked the CLI first rather than assuming the item's three named examples needed matching guardrails: none of them actually has a CLI confirm prompt, because none is genuinely destructive as implemented — `doc restore` always writes a new copy and never overwrites the current document, backup creation is non-destructive by definition, and "removing" a source doesn't exist as an operation (only reversible status changes like ignore/accept); backup *restore* isn't implemented anywhere yet, CLI or web, only create/inspect. Audited the whole web UI for its one actual `DELETE` call and found exactly one hard-to-reverse action: unlinking a Zotero Web API account, which deletes the saved key and requires re-entering it to reconnect. Added a `window.confirm()` there. No Python changes — verified with the full test suite (406 passing, unaffected) and a Node syntax check. Bumped project version to 0.11.30.
- Completed another Phase 26 brainstorm item: a multi-workspace comparison view for anyone running more than one research project at once. Factored `corroborly.api.deps.resolve_workspace`'s `CORROBORLY_WORKSPACE_ROOT` sandbox validation out into a reusable `resolve_workspace_path` function so the new multi-path route validates each workspace the same way single-workspace routes always have, rather than accepting looser path handling just because there are several of them. New `GET /api/v1/projects/compare?workspaces=a&workspaces=b` route (400 `too_few_workspaces` under two paths) reuses `corpus_dashboard_summary` per workspace and adds `project_name`; new `corroborly compare-workspaces <path> <path> ...` CLI command; new "Compare workspaces" web panel (one path per line, side-by-side table). 4 new tests (API, 406 total), verified live (CLI comparing two real workspaces, an end-to-end login+compare-fetch smoke test, template render check) and a Node syntax check. Bumped project version to 0.11.29.
- Completed another Phase 26 brainstorm item: a lightweight local research-progress log — an honest record of research question / artefact activity over time, not a gamified streak feature. Checked first whether this could be derived from existing data and found it couldn't: neither research questions nor artefacts carry any timestamp, so "activity over time" would have to be invented after the fact rather than genuinely recorded. New `corroborly.engine.progress_log` module with an append-only `research-progress-log.yaml`; `approve_research_question`/`reject_research_question`/`archive_research_question` (research_questions.py) and `register_artefact`/`set_artefact_review_status` (artefacts.py) each append an event as the action actually happens, so the log is a real record rather than a derived guess. New `corroborly research-progress` CLI command, `GET /api/v1/reports/research-progress` API route (documented in `docs/api/CONTRACT.md`), and a "Research progress" web panel (reuses the timeline view's timestamp formatting). Caught and fixed a bug during testing: `record_progress_event`'s first call crashed with `FileNotFoundError` because `read_yaml` raises on a missing file and the log file doesn't exist until the first event, unlike every other workspace file which `init_workspace` pre-creates — added the same `path.exists()` guard this codebase's other lazily-created logs already use. 5 new tests (engine + API, 404 total), verified live (CLI end-to-end against a real workspace, a login+render check confirming the new panel appears) and a Node syntax check. Bumped project version to 0.11.28.
- Completed another Phase 26 brainstorm item: a single "hand this to my supervisor" export bundle, distinct from the existing offline evidence bundle (that one is raw YAML for reproducibility/audit; this one is a readable digest for a human reviewer). New `corroborly.engine.export.build_supervisor_bundle` combines a claim-ledger Markdown table (flagging citation gaps and claim-source-validation issues per claim), every citation plan's Markdown created so far, and the workspace review report into one `supervisor-bundle.md` digest, plus `supervisor-bundle.zip` with the raw claims YAML and per-plan Markdown alongside it. Shipped as Markdown + zip rather than PDF — checked first and confirmed no PDF-generation dependency exists anywhere in this project; the digest converts to PDF trivially with any tool the user already has, so this doesn't force a new dependency decision. New `corroborly export-supervisor-bundle` CLI command, `POST /api/v1/export/supervisor-bundle` API route (documented in `docs/api/CONTRACT.md`), and an "Export supervisor bundle" button in the web Export & Reporting panel. 4 new tests (engine + API, 402 total), verified live (CLI against a real workspace with a real claim, confirmed the digest's claim table and fallback messaging) and a Node syntax check. Bumped project version to 0.11.27.
- Completed another Phase 26 brainstorm item: a real chronological research timeline view. `timeline_report` was previously missing two of its four stated event types entirely (terminology changes and context-changelog entries weren't included at all — checked the code before assuming the feature just needed better rendering, and found the underlying report itself was incomplete) and had no way to sort events chronologically, since none of decisions/terminology/feedback/context-changelog carried a timestamp. Added `created_at` tracking to all four (decisions.md and context-changelog.md gained an `- At: <iso>` line in their existing Markdown block format; terminology.yaml and supervisor-feedback.yaml gained a `created_at` field) and rewrote `timeline_report` to merge all five event kinds (run summaries already had `started_at`, now surfaced) into one list sorted by timestamp. Events from before this tracking existed have no confirmed time and sort last rather than being guessed at, consistent with the same approach used for the stale-claims report. The existing web "Timeline report" view (Export & Reporting panel) now shows each event's actual timestamp and text instead of just an ID. Updated `docs/api/CONTRACT.md` and the affected tests (dict-equality assertions that would've broken on the new field), full suite verified (399 passing), plus a live end-to-end smoke test (decision/term/feedback/changelog added via API, timeline fetched and confirmed chronologically ordered with real timestamps) and a Node syntax check. Bumped project version to 0.11.26.
- Completed another Phase 26 brainstorm item: documented keyboard shortcuts, listed in the About modal. Shipped a deliberately small set — `u` (jump to Upload artefacts), `?` (open the shortcuts list), `Esc` (close a dialog, already existed, now documented) — rather than the TODO's illustrative upload/search/accept-ignore trio verbatim: accept/ignore is a per-row table action with no selection concept to attach a global key to, and there's no unified corpus search yet to focus (that's Phase 26's separate, still-open full-text search item). Shortcuts only fire outside text inputs/textareas/selects/contenteditable and without a modifier key held, so they can't misfire while typing a claim or note. No Python changes — verified with the full test suite (399 passing, unaffected) and a live template+JS render/syntax check. Bumped project version to 0.11.25.
- Completed another Phase 26 brainstorm item: a simple local citation-relationship view, no external graph service. New `corroborly.engine.relationships.citation_relationship_map` reads existing links (claim→sources, artefact→sources, artefact→research questions) and reports, per referenced source, which claims it supports and which artefacts use it; each reference is flagged `known: false` if it points at a source/RQ ID that doesn't actually exist in the registry, rather than silently omitting broken links. Deliberately doesn't report a claim→artefact edge — checked the data model first and confirmed artefacts only link to sources and research questions today, not claims, so that edge doesn't exist to report; adding one would be a schema change, tracked as a note in the TODO item rather than invented here. New `corroborly citation-relationships` CLI command, `GET /api/v1/reports/citation-relationships` API route (documented in `docs/api/CONTRACT.md`), and a "Citation relationships" web panel. 2 new engine tests plus an API assertion (399 total), verified live (CLI against a real workspace, an end-to-end login+claim-add+relationship-fetch smoke test confirming the unknown-source flag works) and a Node syntax check. Bumped project version to 0.11.24.
- Completed the last unstarted Phase 26 brainstorm item: a short design note (`docs/ZOTERO_SYNC_DESIGN_NOTE.md`) on whether two-way Zotero sync could ever be done safely. Verified the current boundary first rather than assuming it: `corroborly.engine.zotero` opens the local `zotero.sqlite` exclusively via `_connect_readonly`, and `corroborly.engine.zotero_api` only ever issues `GET` requests — no write path exists anywhere in the codebase today, matching `AGENTS.md`'s explicit "never modify the user's local Zotero directory" rule. The note lays out why write-back is a materially different risk category (local SQLite writes risk corrupting Zotero's own library; the Web API's optimistic-concurrency model needs real conflict handling; a bug here has a larger blast radius than any other Corroborly feature) and what would be non-negotiable if it were ever built (Web API only, granular opt-in, review-before-send, version-checked writes, a local audit log, dry-run mode). Recommends not building it without a concrete use case and its own explicit phase/sign-off, not folding it into an existing one. Explicitly not a commitment to build it, per the TODO item's own framing. Bumped project version to 0.11.23.
- Completed another Phase 26 brainstorm item: corpus dashboard stat tiles. New `corroborly.engine.health.corpus_dashboard_summary` aggregates source counts by status, claim counts by status, artefact count, open (candidate + approved) research question count, and days since last activity (the newest mtime across the workspace's core files — chosen over per-record timestamps so it reflects CLI *and* web/API writes alike without requiring every record type to carry its own clock). New `GET /api/v1/projects/dashboard` route (documented in `docs/api/CONTRACT.md`); the web UI's existing dashboard tiles now pull from it instead of the narrower source-only `/status` route, which stays as-is for existing callers. 2 new tests (engine + API, 397 total), verified live (direct engine call against a real workspace, an end-to-end login+dashboard-fetch smoke test) and a Node syntax check. Bumped project version to 0.11.22.
- Completed another Phase 26 brainstorm item: a "claims not touched in N days" stale-work report, always available with zero AI configured. Claims now carry `created_at`/`updated_at` timestamps (set on `add_claim`, refreshed on `set_claim_status`); new `corroborly.engine.claims.stale_claims`/`write_stale_claims_report` flag open claims (`active`/`needs_evidence`/`needs_review` — settled `supported`/`rejected` claims are excluded on purpose) not updated in at least N days (default 14), each tagged with `age_days` and whether it's also a citation gap. Claims from before this timestamp tracking existed have no confirmed age and are always included rather than silently assumed fresh, consistent with this project's no-silent-gaps stance. New `corroborly claims stale --days N` CLI command, `GET /api/v1/claims/stale?days=N` API route (documented in `docs/api/CONTRACT.md`), and a "Stale claims (14+ days)" button in the web Claims panel. 2 new tests (engine + API), full suite verified (396 passing), plus a live CLI smoke test against a real workspace and a Node syntax check. Bumped project version to 0.11.21.
- Completed another Phase 26 brainstorm item: empty-state next-step guidance across every list view that has an obvious next action (sources, research questions, artefacts, claims, guidelines, decisions/terminology/feedback/context-changelog, uploaded artefacts, personal notes) — each now names the specific button/panel to use instead of a bare "no X yet." Deliberately left a couple of empty states unchanged where no accurate next step exists yet: draft research questions are only added during `corroborly init` today (no post-init add command in CLI, API, or web — verified before writing the message, not guessed), and doc-version/data-source empty states were already accurate descriptions rather than missing-guidance gaps. Verified with the full test suite (395 passing) and a live server smoke test. Bumped project version to 0.11.20.
- Completed two Phase 26 brainstorm items: color-coded status badges (source review status, claim status, artefact review status, upload report rows, feedback rows, RQ readiness, cross-reference candidates) and a dark/light theme toggle for the web UI, respecting system preference by default (`prefers-color-scheme`) with an explicit override stored in `localStorage` and applied before paint on both the main app and login page to avoid a flash of the wrong theme. New shared `statusBadgeClass`/`statusBadgeHtml` helpers in `app.js` replace several near-duplicate inline status-class computations. Fixed an inconsistency caught while finishing this work: the claims table was rendering the new status badge and its review-status `<select>` into the same table cell (artefacts got a dedicated trailing column for its dropdown, claims hadn't) — added the same extra column to the claims table. Verified with the full test suite (395 passing) and a live server smoke test (login page, static assets, JS syntax check). Bumped project version to 0.11.19.
- Documentation/planning only: added TODO Phase 31 (AI Document Validation Diff View With Source-Traceable Suggestions), per Pedro's 2026-07-16 request. Checked against existing TODO content first rather than assuming duplicate or assuming net-new: the deterministic half already exists (`corroborly validate <target>`, Phase 6), and the AI half was previously only gestured at abstractly (Phase 8's "AI edit sessions", Phase 25's `ai review-document` sketch) — this phase makes the concrete UX explicit: validate an uploaded document against the connected Zotero library, a given path, or a direct online API (Scopus, Phase 23), with a side-by-side diff view (original left, AI suggestions right, GitHub/Bitbucket-PR-diff-like) and a per-suggestion popup showing the exact source evidence, so nothing renders as a confident suggestion without a traceable source popup behind it. Entirely AI-gated, explicitly sequenced after Phase 27 (grounding enforcement) for the same reason as Phase 28. Not started.
- Documentation/planning only: added TODO Phase 30 (Audio/Video Transcription Upload and Management), per Pedro's 2026-07-16 request to reuse his sibling project `../transcriber` ("SourceScribe" — verified: local-Whisper-by-default CLI with an optional OpenAI `--ai` mode, outputs plain `transcript.txt` which already matches Phase 25's `import_transcript` passthrough path) rather than reimplementing transcription. Not started; the integration mechanism (dependency vs. subprocess vs. vendoring) is flagged as needing Pedro's explicit decision first, same framing as Phase 24/28.
- Completed Phase 25's deterministic items (the AI-review half stays blocked on the AI opt-in decision). New `corroborly.engine.notes` module and `personal-notes.yaml` workspace store for meeting notes, transcripts, and freeform personal notes — distinct from per-source notes and supervisor feedback. New CLI commands (`corroborly notes add/list/search/tag/import-transcript`) and matching API routes (`GET`/`POST /api/v1/notes*`), plus a basic web UI panel (add form, keyword search, results table) even though only CLI/API were explicitly asked for. Transcript import strips WebVTT/SRT cue numbers and timestamps deterministically, no AI processing at import time. 10 new tests (engine + API, 395 total), verified live via CLI (`corroborly notes add/list/search/tag/import-transcript` against a real workspace) and a running server, plus a Node syntax check. Bumped project version to 0.11.16.
- Completed Phase 19 (Zotero Integration). Wired Web API collection listing/selection into the Zotero panel (checkbox list + "Save selected" once linked). Added local Zotero collection selection as new API routes (`POST /api/v1/zotero/local/collections/select`, `POST /api/v1/zotero/local/use-entire-library`, mirroring the CLI's `select-collections`/`use-entire-library` exactly, including unknown-collection-key validation) with matching web checkboxes/buttons on the local collections list. Added the six remaining read-only Zotero reporting routes (`GET /api/v1/zotero/local/{metadata-report,attachment-health,fulltext-report,duplicates,snapshot,export-bibtex}`) — all previously CLI-only — each replicating the CLI's own output-file write so both surfaces produce the same on-disk artifacts, with web view buttons showing inline summaries. 5 new tests reusing `test_zotero.py`'s existing fake-Zotero-SQLite fixture helpers, verified live end to end (collections, selection, all six reports). Bumped project version to 0.11.15.
- Closed three remaining web UI gaps flagged in earlier phases: a "Create workspace" form (`POST /api/v1/projects/init`, Phase 13) usable before any workspace is loaded, so a new workspace no longer requires the CLI; a manual artefact registration form (`POST /api/v1/artefacts`, for artefacts created outside the tool at an arbitrary path with a free-text type, Phase 15) alongside the existing deterministic-creation form; and an artefact dependency-check button (`GET /api/v1/artefacts/dependencies`, Phase 15) showing an inline summary. No new backend routes — all three routes already existed. Verified live (created a real workspace via the same call the form makes, registered an artefact manually, ran the dependency check) and a Node syntax check.
- Completed Phase 21 (Export and Reporting Tools) and Phase 13's remaining workspace-report item. Added a new `POST /api/v1/export/corpus` route (didn't exist before) plus five more previously CLI-only routes that had zero API coverage: `POST /api/v1/export/merge-pdfs`, `GET /api/v1/reports/schemas`, `GET /api/v1/conversion/ocr-readiness`, `GET /api/v1/conversion/processing-issues`, and `GET /api/v1/sources/watch` — closing a CLI/API parity gap on all six, not just adding web views. New Export & Reporting web panel ties all of it together: evidence bundle export, corpus export, workspace/timeline/schema reports, OCR readiness, processing issues, unregistered-file watch, and PDF merge (with a write/dry-run checkbox matching the CLI's `--write` flag). 4 new tests, verified live against a real workspace. Bumped project version to 0.11.13.
- Added a Data, Metadata & Workspace Admin panel to the web UI (Phase 20), reusing existing `/api/v1/data/*`, `/api/v1/metadata/*`, `/api/v1/conversion/run`, `/api/v1/backup*`, and `/api/v1/db/*` routes end to end — no new backend routes needed. Data sources: list with a profiled/not-profiled badge, status summary, profile-trigger action. Metadata quality: extract/build-index actions plus validation/duplicate report buttons showing inline summary counts. Conversion: run with an "allow OCR" checkbox, processed/converted/skipped/failed summary. Backup: create, and inspect an existing zip by path without restoring it. SQLite admin (init/sync/rebuild/status/pending/apply-pending/privacy): collapsed behind a "power-user" details toggle, matching this area's own lower-priority note in the TODO. Verified with a live HTTP smoke test covering all five sub-areas against a real CSV data source (scan, list, status, profile, extract, index, validate, duplicates, convert, backup, inspect, and every db admin action) and a Node syntax check. Bumped project version to 0.11.12.
- Renamed the project from ResearchBoss to Corroborly, at Pedro's request (2026-07-16), for a friendlier, more research-industry-suggestive name. Renamed the Python package directory (`researchboss/` → `corroborly/`), the CLI command (`researchboss` → `corroborly`), the PyPI-style package name and entry point in `pyproject.toml`, every `RESEARCHBOSS_*` env var (`CORROBORLY_API_USERNAME`, `CORROBORLY_API_PASSWORD`, `CORROBORLY_API_SESSION_HOURS`, `CORROBORLY_WORKSPACE_ROOT`, `CORROBORLY_UPLOAD_MAX_FILES`, `CORROBORLY_UPLOAD_MAX_FILE_SIZE_MB`), the session cookie name (`corroborly_session`), the Docker Compose service/container name (`corroborly-api`), and all web UI branding (page titles, the About modal). Applied as a systematic find-and-replace across all 1,210 occurrences in 107 tracked files, verified with zero occurrences of the old name remaining anywhere in the repo, a clean editable reinstall (`pip install -e .`) producing a working `corroborly` command, the full test suite passing unmodified in behavior (376 tests), and a live HTTP smoke test against a running server using the new env var names. Bumped project version to 0.11.11.
- Added a Document Vault & Version History panel to the web UI (Phase 18), reusing existing `/api/v1/doc/*` routes end to end — no new backend routes needed. Snapshot any document target, browse its version list (optionally filtered by target), diff two versions as a unified-diff code block, compare two versions' linked validation reports (strengths/weaknesses/unsupported-claims/references as added/removed lists, with an explicit "not comparable" state when either version has no linked report), and restore an older version as a new copy via a per-row button. Verified with a live HTTP smoke test against a real two-version document (snapshot, diff, the not-comparable case, restore) and a Node syntax check. Bumped project version to 0.11.10.
- Added Guidelines and Project Log panels to the web UI (Phase 17). Guidelines reuses existing routes end to end: list, a registration form, a "save checked as defaults" action, and a conflict-report button. Project Log needed real backend work first — decisions/terminology/feedback/context-changelog were POST-only with no way to read anything back via either the API or the CLI. Added `GET /api/v1/decisions`, `GET /api/v1/terminology`, `GET /api/v1/feedback`, `GET /api/v1/context/changelog` (backed by new `engine.project_log.list_decisions/list_terminology/list_feedback/list_context_changes`) and matching CLI `decisions list`/`terminology list`/`feedback list`/`context list` commands, closing a CLI/API parity gap that existed on both surfaces, not just the web UI. `list_decisions` now returns structured `{id, decision, reason}` records instead of bare ID strings (nothing outside the module depended on the old shape). 5 new tests (2 engine, verified via live smoke test for the rest) plus a Node syntax check. Bumped project version to 0.11.9.
- Added Claims and Citation Planning panels to the web UI (Phase 16), reusing existing `/api/v1/claims/*` and `/api/v1/citations/*` routes end to end — no new backend routes needed. Claims: a table with a per-row status dropdown, an add-claim form, and citation-gap/claim-source-validation report buttons that show a summary count. Citation planning: enter a document target, create a deterministic plan, review each proposed insertion with Accept/Reject, then apply the accepted ones — this is the citation half of Phase 10's originally-scoped review-overlay work (only the cross-reference half had a web view before this). Also refactored three near-duplicate `<details>` block CSS rules (source scan, artefact create, Zotero link) plus the new claim-add one into a single shared `.detail-box` class. Verified with a live HTTP smoke test (add/status a claim, run both reports, create a real citation plan against a generated artefact including the zero-insertions case, apply it) and a Node syntax check on the new JS. Bumped project version to 0.11.8.
- Added Research Questions and Artefact Registry panels to the web UI (Phase 15), reusing existing `/api/v1/rqs/*` and `/api/v1/artefacts/*` routes end to end — no new backend routes needed. Research questions render as three grouped lists (candidates/approved/rejected), each item showing its question, subquestions, and (once checked) a deterministic readiness status+score badge; approve is only offered on candidates and reject/archive on candidates+approved, matching what `engine.research_questions` actually allows (approving an already-approved RQ isn't a real operation). Artefact registry shows a table with a per-row review-status dropdown and a deterministic artefact-creation form (type select, optional title, include-maybe checkbox) over `POST /api/v1/artefacts/create`. Manual artefact registration (`POST /api/v1/artefacts`, for artefacts created outside this tool) and the dependency-check view (`GET /api/v1/artefacts/dependencies`) don't have web views yet — smaller remaining follow-ups. Verified with a live HTTP smoke test (create a workspace with a draft RQ, check readiness, approve it, create a research-question-brief artefact linked to it, change its review status) and a Node syntax check on the new JS. Bumped project version to 0.11.7.
- Upgraded login from a bare password box to username+password, per Pedro's request to match `../Resilinked`'s login UX. New `CORROBORLY_API_USERNAME` env var required alongside the existing `CORROBORLY_API_PASSWORD` (`auth_configured()` now requires both, `verify_password` replaced with constant-time `verify_credentials(username, password)`); `POST /api/v1/auth/login` now takes `{"username", "password"}` instead of `{"password"}`, and the wrong-credential response no longer distinguishes which field was wrong. Still a single shared credential pair (not real per-user accounts) — updated `.env.example`, `docker-compose.yml` (both vars now required via Compose's `:?` syntax), `docs/api/CONTRACT.md`, `docs/DEPLOY.md`, and README to match, and added TODO Phase 29 (Multi-Tenant Support for Commercial Deployment, not started, explicit business decision needed before building) for the real per-account/per-workspace-isolation feature this explicitly is not. Updated all existing auth tests (2 new: wrong-username rejection, auth-not-configured-when-only-password-set) plus a live HTTP smoke test confirming the old password-only request body now 422s and the new username+password body works. Also fixed a stale "UI is not implemented yet" line in README's Known Gaps left over from before Phase 10 shipped. Bumped project version to 0.11.6.
- Added a workspace dashboard panel and a Sources management panel to the web UI (Phase 13/14), reusing existing `/api/v1/projects/*` and `/api/v1/sources/*` routes end to end — no new backend routes needed. Dashboard: stat tiles from `GET /api/v1/projects/status`, a health status pill from `GET /api/v1/projects/health` with a detail line listing missing files/dirs, failed conversions, and unsupported files when not "ok". Sources: a filterable table (`GET /api/v1/sources?status=`) with accept/maybe/ignore actions per row, a note+tag edit modal, and a folder-scan form — the source inbox/review workflow (one of the most-used CLI command groups) had zero web UI coverage before this. Also persisted the last-used workspace path in browser `localStorage` (falls back only when no `?workspace=` URL param is present, so bookmarks/shared links still win) so returning to `/` doesn't require retyping the path. Revised Phase 13's framing: adding these as flat stacked panels (same pattern as the Zotero panel) worked fine without first building the persistent nav shell that phase originally assumed was a blocker — that item stays open for whenever the page gets crowded enough to need it. Verified with a live HTTP smoke test against a running server (login, scan a real folder, list, change status, set note, add tag, confirm dashboard counts update) and a Node syntax check on the new JS. Bumped project version to 0.11.5.
- Documentation/planning only: added TODO Phase 28 (Guided Research-Question Elicitation and AI-Assisted Paper Drafting), stating a core project goal Pedro described 2026-07-16 — going from a vague topic to a refined, falsifiable research question (a claim framed to be proved or disproved by default) via a guided open-answer/multiple-choice wizard, and from there to a paper draft that's AI-drafted prose if requested but always gated behind a mandatory deterministic review flag (`corroborly validate <target>`) before anything counts as final. Explicitly sequenced after Phase 27 (AI Output Grounding and Anti-Hallucination Enforcement) — whole-paper AI drafting is this project's highest-stakes surface for the Core Rule, not something to build with looser grounding in parallel. Added matching goal statements to README's Project Goals and AGENTS.md's Project Intent so this reads as a stated goal, not just a buried TODO item. Bumped project version to 0.11.4.
- Added Zotero Web API account linking from the web UI, not just CLI discovery, per Pedro's 2026-07-16 request. New `POST`/`DELETE /api/v1/zotero/api/credentials` routes (`engine.zotero_api.save_zotero_api_credentials`/`clear_zotero_api_credentials`) save/remove `ZOTERO_API_KEY`/`ZOTERO_USER_ID` into the workspace's local `.env` via a new `write_dotenv_values`/`remove_dotenv_values` helper that preserves every other line in the file; the submitted key is never echoed, logged, or returned by any route — only a `configured: true/false` confirmation, consistent with the existing "API keys must not be returned, printed, or logged" boundary in `docs/api/CONTRACT.md`. Added matching CLI commands (`corroborly zotero api-link`/`api-unlink`, hidden-input prompt when the key isn't passed as a flag) for CLI/API parity, since linking previously required hand-editing `.env` from either surface. Added a Zotero panel to the web UI (`corroborly/web/`) with: a link/unlink form, live status pills for local storage detection and Web API connection, a local-storage keyword search box, and a local collections list with item counts — a basic, read-only "perusal" view so day-to-day reference lookup doesn't require switching to the Zotero app. 8 new tests (engine dotenv round-trip, API route, CLI command) plus a live HTTP smoke test against a running `corroborly serve` instance (login, save/verify/.env-inspect/delete) and a Node syntax check on the new JS. Bumped project version to 0.11.3.
- Added a new non-negotiable core rule to `AGENTS.md`, at Pedro's explicit direction: the deterministic core must always work fully with zero AI configured (already true by design, now stated as a permanent invariant, not just a current-phase constraint), and whenever an AI provider API key is active, AI output must never fabricate — every factual claim must trace to real accepted-source text, artefacts, claims, or the user's own notes, with explicit "insufficient evidence" as a required, valid response rather than a plausible-sounding guess. Cross-referenced from `docs/api/CONTRACT.md`'s Non-Negotiable Boundaries and Future AI Routes sections (added an `insufficient_evidence` envelope-field placeholder) and from every AI-tagged TODO phase (5, 22, 23, 25). Added TODO Phase 27 (AI Output Grounding and Anti-Hallucination Enforcement) as the actual engineering mechanism — grounding checks, provenance tagging of AI vs. human text, adversarial off-corpus regression tests, and a zero-AI-configured CI check — that every other AI feature phase now depends on before it can ship.
- Set up a project-scoped `PostToolUse` hook (`.claude/settings.local.json`, installed after explicit approval) that regenerates `TODO.html` via the existing `todo-ledger` generator script whenever `TODO.md` is edited, so the dashboard view stays live without manual regeneration.
- Genericized the repo so it's usable by anyone who clones it, not just Pedro's own setup: rewrote `docs/DEPLOY.md` as a domain/host-agnostic Docker Compose deploy guide (was hardcoded to `research.veloso.dev` and a specific sibling deploy-tool project); moved those personal specifics into a new gitignored `docs/DEPLOY.personal.md`; genericized TODO.md/DETAILED_ROADMAP.md's Phase 12 and the README status line to match. LICENSE/README author+email and the LICENSE copyright name are kept as the intentional, expected personal references (standard for an MIT-licensed repo). Added an optional PayPal donate link (README Author section and the web UI About/License modal) using `pedro@veloso.dev` as the PayPal `business` param — no third-party service integration, just a link.
- Confirmed and documented the current database setup for anyone asking "does this need MariaDB or SQLite": SQLite only today, as an optional local index/cache layer over YAML/Markdown (the actual source of truth) — already zero-config-portable across machines via the startup scripts, no server DB required.
- Bumped project version to 0.11.2.
- Brainstormed and added 3 new TODO.md phases (24-26, 25 items), based on Pedro's direction: Phase 24 specs an optional secondary database backend (MariaDB or PostgreSQL, mutually exclusive, kept in sync with SQLite via CLI/web opt-in prompts and bidirectional repair, with SQLite remaining the mandatory always-on default — never zero backends active) without reversing the project's local-first/no-required-remote-DB principle; Phase 25 specs AI-assisted working-document review grounded in the full evidence base plus a new meeting-notes/transcript/personal-notes evidence store, blocked on the same AI opt-in decision as Phase 22; Phase 26 is a curated brainstorm of generically valuable research-tool features (corpus dashboard, empty states, document gallery, status badges, dark mode, timeline view, export bundles, etc.), a few conceptually inspired — never code/text-copied — by UI/UX patterns from Pedro's unrelated `../Resilinked` and `../zqx` projects. None of Phase 24-26 is implemented yet; these are planning-only tickets.
- Documentation only, no code changes: brainstormed and added 11 new TODO.md phases (13-23, 40 items) inventorying every remaining CLI command group that has no web UI view yet — sources, artefacts, research questions, claims, citation planning, guidelines, project log, document version history, Zotero integration, data/admin tooling, export/reporting, and the AI-assisted/external-search features that stay explicitly blocked on the same AI opt-in decision deferred elsewhere in this file. Each item names the existing API route it would use, or says explicitly that a new route is needed first (verified, not guessed — checked which CLI commands actually have no API route yet: `export-corpus`, `merge-pdfs`, `watch`, `report-schemas`, `abstracts import`, most Zotero reporting commands, and local Zotero collection selection all have none today). Phase numbering is for organization, not a strict priority order.
- Bumped project version to 0.11.1.

- Added the Phase 10 web UI (`corroborly/web/`), mounted on the same FastAPI app `corroborly serve` already runs. Decision: FastAPI + Jinja2 server-rendered shell + vanilla JavaScript, no build toolchain, no third-party JS/CSS dependency, no CDN assets — React/Vue/Svelte/Flutter were considered and passed on. The web layer has no import path to `corroborly.engine`: it only imports session-cookie helpers from `corroborly.api.auth`, and every data operation in `static/app.js` is a `fetch()` call to an existing, tested `/api/v1/*` route — the web UI is architecturally just another API client.
  - `GET /login` (public) and `GET /` (server-side session-gated: reads the session cookie directly and 303-redirects to `/login?next=<url>` before rendering anything, not just after a failed client-side call).
  - Drag-and-drop batch upload with pre-submission limits (new `GET /api/v1/artefacts/upload/limits` route — max files, max size, allowed extensions — so the UI can show them before the user picks files, not just react to a 400 after) and a per-file batch-results table.
  - A popup preview modal (PDF via `<iframe>`, text/Markdown/CSV/JSON fetched into `<pre>`, unsupported types get an "open in a new tab" fallback instead of a broken embed), dismissible by Escape, close button, or click-outside.
  - A cross-reference review overlay (accept/reject each candidate via the existing `candidate-review` route, then apply) — the artefact-upload half of Phase 10's review UI requirement.
  - An About/License footer modal with the MIT notice and author contact info, sourced from `LICENSE`/README.
  - Reuses the already-built `GET /api/v1/artefacts/uploads` (list) and `GET /api/v1/artefacts/uploads/{upload_id}/file` (inline preview bytes) routes from the previous entry below.
  - Found and fixed two real bugs while verifying this against an actual `pip install` rather than just the source-tree test suite: (1) a circular import — `api/__init__.py` eagerly re-exported `create_app`, which round-tripped through `web/app.py` back into `api.auth`; every test/smoke script in this repo happened to always import `corroborly.api.app` first, which sidesteps the failing order, so this was latent until a genuinely fresh install was tried. Fixed by emptying the unused re-export. (2) `pyproject.toml` had no `package-data` entry, so a real wheel wouldn't have shipped `corroborly/web/templates/`or `static/` at all — added `[tool.setuptools.package-data]`. Both confirmed fixed by building a real wheel, installing it into a throwaway venv, and testing both import orderings plus the CLI entry point and `corroborly doctor` from that clean install.
  - `docs/PACKAGING.md`'s conditional Flutter desktop-sidecar section is now historical/moot.
- Bumped project version to 0.11.0.

- Added `POST /api/v1/citations/plan/insertion-review` (and `corroborly cite review` CLI equivalent) to set one citation-plan insertion's review_status via API/CLI instead of hand-editing the plan YAML, closing the identical gap just flagged for cross-reference candidates in the previous entry. New `citations.set_citation_plan_insertion_review_status` engine function, keyed by `(sentence_index, source_id)` — the same pair `create_citation_plan` builds each insertion from. This router has no dynamic-segment routes, so there was no risk of the same collision bug, but verified via the OpenAPI schema and a live HTTP smoke test of the full plan → insertion-review → apply flow anyway. Both major review-before-apply flows (cross-reference and citations) are now fully reachable from a browser client with zero filesystem access required.
- Bumped project version to 0.10.0.

- Added `POST /api/v1/artefacts/cross-reference/candidate-review` (and `corroborly doc cross-reference-review` CLI equivalent) to set one cross-reference candidate's review_status via API/CLI instead of requiring a hand-edited YAML file — the missing piece for a browser-based reviewer, found during the same Phase 10 UI-readiness check as the upload-listing/file-preview routes below; citation plans have the identical hand-edit-then-apply limitation and don't have the equivalent yet, noted as a follow-on gap in TODO.md. New `cross_reference.set_cross_reference_candidate_review_status` engine function only touches the one matched candidate. Caught and fixed a real routing bug while building this: naming it `.../cross-reference/review` exactly matched the pre-existing `POST /api/v1/artefacts/{artefact_id}/review` path pattern (`artefact_id` literally `"cross-reference"`), and since that route registers first, FastAPI validated the request against the wrong Pydantic model and returned an unexpected 422 instead of ever reaching the new handler — found via a live HTTP smoke test. Renamed to `candidate-review` to remove the collision outright rather than depend on registration order. Verified with unit tests (engine + API + CLI) and a live HTTP smoke test of the full candidates → candidate-review → apply flow with zero filesystem hand-editing.
- Bumped project version to 0.9.9.
- Added `GET /api/v1/artefacts/uploads` (list previously uploaded artefacts) and `GET /api/v1/artefacts/uploads/{upload_id}/file` (serve an uploaded artefact's renamed vault copy inline for browser preview, `Content-Disposition: inline`). Found both missing during a Phase 10 UI-readiness check: every other route returns the JSON envelope, and nothing served raw file bytes at all — a real, framework-independent blocker for the planned popup preview view. New `vault.resolve_uploaded_artefact_file` engine function re-validates the resolved path stays inside `document_vault/` after resolving, so a hand-edited ledger record can't be used to read an arbitrary file off disk. Verified with a live HTTP smoke test (login, upload, list, fetch file, confirm `Content-Disposition: inline`/content-type, confirm 404 for an unknown upload_id). Noted a real follow-on gap this surfaced: `sources.ALLOWED_EXTENSIONS` (shared by uploads) has no image extensions, so image preview isn't reachable yet — left as a TODO note rather than silently expanding the allow-list, since it also affects source-folder scanning.
- Bumped project version to 0.9.8.
- Documented shape-only sketches (no implementation, no route registration) for the four Future AI Routes (`POST /api/v1/ai/{test,review,novelty,rqs/assess}`) in `docs/api/CONTRACT.md`, modeled directly on their existing CLI equivalents (`ai test`, `ai review`, `assess-novelty`, `rqs assess`) and `engine.ai` functions so a future implementer has an unambiguous request/response contract to build against. Key documented decision: the API-level AI opt-in is a required `"ai": true` request-body field per call, mirroring the CLI's per-invocation `--ai` flag, not a workspace- or session-wide toggle; the OpenAI API key stays server-side only (resolved the same way the CLI resolves it) and must never be accepted from or echoed to the client. Also resolves where novelty assessment belongs in the contract: `POST /api/v1/ai/novelty`, not a separate `/api/v1/novelty` route implying a deterministic engine path that doesn't exist.
- Bumped project version to 0.9.7.
- Added `corroborly doc cross-reference <upload_id>` and `corroborly doc cross-reference-apply <upload_id>` CLI commands, found missing during a full API-route/CLI-command parity audit (every implemented API route is documented in CONTRACT.md and vice versa except the disabled Future AI routes — confirmed via the OpenAPI schema — but `engine.cross_reference`'s deterministic functions only had API routes, not CLI ones; the sole "cross-reference" CLI command was an unrelated older `--ai`-gated one). Verified with a live end-to-end CLI smoke test.
- Bumped project version to 0.9.6.
- Added derived text snapshots with paragraph/sentence anchors for document versions (`corroborly.engine.derived_text`, `corroborly doc derive-text`, `POST /api/v1/doc/derive-text/{version_id}`) — the anchor infrastructure future AI-assisted editing needs, built ahead of that AI feature itself. Each sentence gets a `citation_insertion_anchor`, plus `claim_ids` and `reference_ids` when a linked validation report exists. Section maps only work for `.md` targets; `.txt`/`.docx`/`.pdf` get no section detection rather than a guessed one, since there's no reliable structural marker in their extracted plain text.
- Fixed a real pre-existing bug found while building the above: `conversion._markdown_to_text`'s heading/blockquote/list-stripping regexes used `\s` (matches newlines) for leading same-line indentation, letting a match starting at a preceding blank line reach forward across it and silently merge that block into the previous paragraph. Fixed to `[ \t]`; added a dedicated regression test. Affects all markdown conversion, not just the new feature.
- Bumped project version to 0.9.5.
- Added `POST /api/v1/artefacts/cross-reference/apply`, resolving the open design question from the candidates route: reviewed links (`review_status: accepted`/`approved` in the persisted report) are written as metadata on the *upload* record (`cross_references`, mirroring `linked_sources`), never as text inserted into artefact/source/claim documents — a keyword-overlap match is weaker evidence than a validated missing-citation match, so auto-inserting text would be a worse default than reviewable metadata. Idempotent on re-apply. New `vault.add_cross_references_to_upload` engine function. Verified live over real HTTP end to end (upload, get candidates, hand-approve, apply, confirm the artefact record itself was untouched).
- Bumped project version to 0.9.4.
- Added `Dockerfile`, `docker-compose.yml`, and `docs/DEPLOY.md` for deploying the local API to a NAS via `../synology-site-deployer` (unmodified) as `research.veloso.dev`. Single-process by design (session state is in-memory, so multiple workers/replicas would randomly fail logins); bind-mounts a workspace root matching `CORROBORLY_WORKSPACE_ROOT`; extended the existing `.env.example` rather than inventing a second env-file convention. Verified the Python packaging and exact container `CMD` in a clean venv (serves `/health` successfully); the container build itself was not verified — no Docker available in this environment — and the docs say so rather than implying otherwise. Nothing has actually been deployed; that step needs real NAS/Cloudflare credentials.
- Bumped project version to 0.9.3.
- Added `docs/PACKAGING.md` (Phase 11 plan): distribution approaches, a PyInstaller recipe with known uvicorn/`python-multipart` hidden-import gotchas, conditional Flutter desktop sidecar notes (only relevant if Phase 10 picks Flutter), and platform considerations. No packaged build produced or tested yet — planning only.
- Fixed a real gap found while writing the platform-considerations section: `workspace.zotero_storage_candidates()` had no Linux branch at all (`corroborly init` could never auto-detect a Linux Zotero install). Added native (`~/Zotero/storage`, `~/.zotero/zotero/*/zotero/storage`) and Flatpak (`~/.var/app/org.zotero.Zotero/data/zotero/storage`) candidates.
- Bumped project version to 0.9.2.
- Added `GET /api/v1/artefacts/cross-reference` (new `corroborly.engine.cross_reference` module): proposes deterministic links between an uploaded artefact and existing artefacts, sources, and claims by keyword overlap in titles/filenames (claim matches require a stronger overlap). Read-only — writes a candidate report but never an artefact, source, or claim record. The `apply` (write-back) route is deliberately not built yet: whether "write the link" means artefact-registry metadata or literal document-content insertion is a real design decision, not inferable from the citation-plan precedent.
- Bumped project version to 0.9.1.
- Added `POST /api/v1/artefacts/upload` for batch artefact uploads: multipart file bytes streamed to a bounded-size temp location (never buffered whole in memory), rejecting the whole batch with `400 upload_batch_too_large` before writing anything if it exceeds `CORROBORLY_UPLOAD_MAX_FILES` (default 25), per-file `CORROBORLY_UPLOAD_MAX_FILE_SIZE_MB` (default 50) and extension allow-list enforcement, content-hash duplicate detection, and a per-batch report persisted to `outputs/validation/upload-batch-report.yaml`. Backed by a new `vault.intake_uploaded_artefact_batch` engine function shared with (but not yet exposed by) the CLI.
- Bumped project version to 0.9.0.
- Added `CORROBORLY_WORKSPACE_ROOT` so a deployed API instance can be pointed at a mounted volume (e.g. a NAS bind-mount): every `workspace` value must then resolve inside that root, closing a path-traversal gap where any absolute path reachable by the server process was previously accepted as a "workspace." Unset, behavior is unchanged (local-first, any absolute path).
- Bumped project version to 0.8.4.
- Added uploaded-artefact intake to the document vault through `corroborly doc upload/uploads`: copies an externally created file into `document_vault/uploads/{originals,renamed}` without ever modifying the upload itself, using a shared `corroborly.engine.filenames` module (extracted from the existing source filename-suggestion logic) for the renamed copy's author/year/title tokens, plus an embedded upload ID that keeps renamed copies collision-free and a numeric-suffix fallback for same-named original copies.
- Bumped project version to 0.8.3.
- Added `POST /api/v1/validation/run`, `POST /api/v1/citations/plan|apply`, `GET/POST /api/v1/guidelines/*`, and `GET/POST /api/v1/db/*` routes to the local FastAPI backend — every route in `docs/api/CONTRACT.md` is now implemented except the disabled Future AI Routes section and novelty assessment (which has no deterministic engine path and needs explicit AI opt-in rules, not just a contract addition).
- Bumped project version to 0.8.2.
- Added single-user login protection to the local FastAPI backend: `POST /api/v1/auth/login`/`logout`, an in-memory expiring session store, and a `require_session` dependency guarding every `/api/v1` route except login itself. Fails closed (`503 auth_not_configured`) rather than allowing unauthenticated access when `CORROBORLY_API_PASSWORD` is unset. Sessions support both cookie and `Authorization: Bearer` token auth and are never persisted to YAML, SQLite, or git.
- Bumped project version to 0.8.1.
- Completed every route documented in `docs/api/CONTRACT.md` (except the disabled Future AI Routes section): conversion, metadata, data, claims, artefact creation, Zotero (read-only local and Web API, with collection selection written only to the workspace), reports, evidence export, backup, and project log routes.
- Moved Zotero workspace-config resolution (`resolve_zotero_paths`, `configured_source_root`, `configured_zotero`, `write_zotero_config`) from private `cli.py` helpers into `corroborly.engine.zotero` so the CLI and the new Zotero API routes share the same logic instead of duplicating it.
- Bumped project version to 0.8.0.
- Added `GET/POST /api/v1/sources/*`, `GET/POST /api/v1/artefacts/*`, and `GET/POST /api/v1/rqs/*` routes to the local FastAPI backend, all reusing existing tested engine functions with 404s for unknown source/artefact/RQ IDs rather than generic 400s.
- Bumped project version to 0.7.2.
- Started Phase 9 local FastAPI backend (`corroborly.api`, run with `corroborly serve`): app factory with a shared response envelope and error handling, plus `GET /health` (no workspace/auth dependency), `GET/POST /api/v1/projects/*`, and the `POST/GET /api/v1/doc/*` document-versioning routes, all reusing existing tested engine functions.
- Bumped project version to 0.7.1.
- Added Phase 8 local document vault, versioning, and restoration through `corroborly doc version/versions/diff/restore/compare`, with a `document-vault.yaml` ledger and a `document_vault/` folder for originals, versions, diffs, and manifests.
- Added automatic pre/post-change document version snapshots to `corroborly cite apply`, linking each applied citation copy to its source snapshot, validation report ID, and citation plan ID.
- Added `document_versions` SQLite sync so `corroborly db sync` indexes document vault version history.
- Bumped project version to 0.7.0.
- Added explicit SQLite tables for validation runs, evidence matches, citation plans, guideline registrations, search query history, and document version metadata.
- Bumped project version to 0.6.1.
- Added Phase 7 workspace SQLite memory, indexing, and sync through `corroborly db init/sync/status/rebuild`.
- Added reviewed SQLite-to-YAML/Markdown pending-change commands through `corroborly db apply-pending --review` and `corroborly db apply-pending --apply`.
- Added SQLite memory defaults, document aliases, bounded FTS indexes, integrity/rebuild checks, and database privacy checks.
- Bumped project version to 0.6.0.
- Added AI-assisted citation planning through `corroborly cite ai-plan --ai`, with an explicit `--full-target-document-ai` opt-in for whole-document AI use, and safe AI guideline context preparation through `corroborly guidelines ai-context --ai`.
- Added Scopus metric provenance to quality-scored external candidate reports.
- Added AI candidate validation review through `corroborly search ai-candidate-review --ai --external-search` and AI external query planning through `corroborly search ai-query-plan --ai --external-search`.
- Added AI abstract-screening reports through `corroborly ai abstract-screening --ai`, alongside explicit full-context AI opt-in gates for whole-document and whole-source-document AI use.
- Added local processing-issue reports through `corroborly processing-issues` and local abstract candidate import through `corroborly abstracts import`.
- Added local PDF merge manifests through `corroborly merge-pdfs`, deterministic sidecar metadata import through `corroborly metadata sidecars`, an explicit local OCR fallback for scanned PDFs through `corroborly convert --ocr`, optional local PDF text extractors, and deterministic filename suggestions through `corroborly metadata filename-suggestions`.
- Added accepted-source corpus export through `corroborly export-corpus`, local Zotero candidate matching, and reviewed external candidate imports through `corroborly search import-candidates`.
- Added deterministic citation plans and reviewed citation-plan application through `corroborly cite plan` and `corroborly cite apply`, expanded citation application formats, and citation candidate safety gates that require `--allow-candidate-citations` before suggesting citations from non-accepted sources.
- Added report schema guidance through `corroborly report-schemas`.
- Added guideline conflict reports through `corroborly guidelines conflicts` and guideline defaults/precedence through `corroborly guidelines defaults`.
- Added deterministic document target resolution and `corroborly validate <target>` reports with strengths, weaknesses, unsupported or weakly supported sentences, citation gaps, confidence factors, confidence scores, and APA7 references.
- Added guideline registration through `corroborly guidelines add/list`, with workspace-local snapshots, extracted text, and validated scopes.
- Bumped project version to 0.5.4.
- Added structured external-search query plans with legacy params-file import, broad/balanced/strict strategy modes, query group labels, and research-question links.
- Bumped project version to 0.5.3.
- Added Scopus quality scoring, threshold filtering, query validation reports, external-paper candidate registers, metadata-only full-text availability signals, and no-result or low-result query logs.
- Bumped project version to 0.5.2.
- Added explicit Scopus external-search foundation with query planning, query history, readiness checks, response snapshots, no-results logs, and `--external-search` opt-in.
- Added safe AI workspace reports for corpus summaries, claim-check assistance, citation-gap recommendations, artefact cross-reference, and source-relevance recommendations.
- Bumped project version to 0.5.1.
- Added AI-assisted review, novelty assessment, and research-question assessment commands behind explicit `--ai` opt-in, with local report outputs and mocked privacy-boundary tests.
- Added TODO and roadmap lists for future AI work that still makes sense to implement.
- Bumped project version to 0.5.0.
- Added Phase 5 OpenAI foundation: `corroborly ai test`, environment/`.env` key loading, explicit `--ai` live-check opt-in, safe context preview generation, and privacy-boundary tests.
- Bumped project version to 0.4.1.
- Added `docs/api/CONTRACT.md` with the planned local FastAPI boundary, endpoint groups, shared-engine expectations, read-only Zotero constraints, and disabled future AI route rules.
- Bumped project version to 0.4.0.
- Added optional read-only Zotero Web API support for credential testing, collection listing, and workspace collection selection.
- Added `ZOTERO_API_KEY` and `ZOTERO_USER_ID` placeholders to `.env.example`.
- Bumped project version to 0.3.9.
- Completed current Phase 4 offline deterministic TODO items that do not require AI or API implementation.
- Added research question templates for all project types, warning thresholds, source notes/tags, source review reports, claim status workflow, claim-source validation, structured decisions, terminology, feedback, context changelog, and local timeline reports.
- Kept pending AI/API TODO items color-marked separately.
- Bumped project version to 0.3.8.
- Completed current Phase 1, Phase 2, and Phase 3 offline deterministic TODO items that do not require AI or API implementation.
- Added scan provider validation, workspace health reports, backup dry-run inspection, DOI/citation consistency validation, metadata duplicate reports, converted-text keyword indexing, evidence bundle export, artefact review statuses, and artefact dependency checks.
- Color-marked pending TODO items that require AI implementation or API development.
- Bumped project version to 0.3.7.
- Added richer read-only local Zotero SQLite coverage for notes, tags, relations, linked items, and extra metadata fields.
- Expanded deterministic Zotero search, snapshots, metadata-quality reports, and BibTeX export to use the richer offline metadata.
- Bumped project version to 0.3.6.
- Added offline deterministic roadmap/TODO items for DOI validation, citation consistency, duplicate metadata reports, source review reports, source notes/tags, claim validation, artefact review, evidence bundle export, project-log commands, workspace health, and backup restore dry-run.
- Bumped project version to 0.3.5.
- Added deterministic `corroborly rqs check` readiness checks with local validation reports and per-question readiness metadata.
- Documented that novelty, contribution strength, field usefulness, and evidence-quality certainty remain human-review or future AI-assisted concerns.
- Bumped project version to 0.3.4.
- Added the roadmap rule that anything not safely deterministic belongs to the later AI implementation phase with explicit opt-in and privacy-boundary tests.
- Bumped project version to 0.3.3.
- Added deterministic `corroborly artefacts create` workflows for source summaries, literature review matrices, claim-evidence tables, research question briefs, and data profile summaries.
- Ensured generated artefacts are non-AI, workspace-only, exclude ignored sources by default, and require user review.
- Bumped project version to 0.3.2.
- Added strict one-way Zotero-to-Corroborly config flags and guards that block writes inside the local Zotero directory.
- Added roadmap notes for future explicit AI options to read whole files or directories for full-paper reasoning and artefact cross-reference workflows.
- Bumped project version to 0.3.1.
- Changed init citation style wording to Zotero-style titles, including explicit `American Psychological Association 7th edition`.
- Added read-only CSL style title parsing for local Zotero/CSL style files.
- Documented the hard rule that Corroborly and any future AI feature must never modify the local Zotero directory.
- Bumped project version to 0.3.0.
- Added deterministic conversion for TXT, MD, DOCX, and simple page-marked PDF text extraction.
- Added conversion cache keyed by source hash and failed conversion records under `sources_failed/`.
- Added deterministic citation metadata extraction with DOI/year/title handling and no invented metadata.
- Added local CSV, SQLite, and JSON data profiling with reports under `outputs/data-profiles/`.
- Added M.Phil and PhD research stage templates.
- Added research question list, approve, reject, and archive workflows.
- Added artefact registry commands with linked source IDs, linked research question IDs, review flags, and AI-generated flags.
- Added manual claim ledger commands and citation gap reports.
- Added local Markdown workspace report generation.
- Added one-shot source watch candidate reports for unregistered files.
- Added local workspace zip backups with original sources excluded by default.
- Added workspace config migration and schema versioning.
- Bumped project version to 0.2.0.
- Added offline Zotero SQLite metadata support through read-only immutable local database connections.
- Added Zotero parent-root config when a `storage/` folder is selected.
- Added local Zotero collection listing, selected-collection configuration, one-off collection scans, and entire-library mode.
- Added local Zotero metadata quality, attachment health, full-text cache, duplicate candidate, metadata snapshot, and BibTeX export commands.
- Enriched Zotero source records with local metadata when available.
- Extended deterministic Zotero search to score title, creators, abstract, collection paths, and DOI from local SQLite metadata.
- Bumped project version to 0.1.1.
- Added `DETAILED_ROADMAP.md` as the living 17-section implementation roadmap.
- Added an explicit current version line to the README.
- Added read-only local Zotero storage search over filenames and `.zotero-ft-cache` text without AI or Zotero API use.
- Added Zotero storage metadata to scanned source records: storage key, relative path, and full-text-cache presence.
- Updated `corroborly scan` to inherit `zotero_storage` provider mode from workspace config when `--kind` is omitted.
- Phase 2 work is planned next: TXT/MD conversion, conversion caching, and citation metadata extraction.

## 0.1.0 - 2026-07-03

Phase 1 engine and CLI foundation completed.

Added:

- Local-first workspace initialization.
- Default YAML and Markdown workspace state files.
- Source folder skeletons for original files, converted text, metadata, failed sources, artefacts, outputs, logs, and context versions.
- Typer CLI command layer with `init`, `doctor`, `version`, `status`, `config validate`, `scan`, and `sources` commands.
- Runtime readiness checks before `corroborly init` and through `corroborly doctor`.
- `python -m corroborly` entrypoint coverage.
- Local source scanning for PDF, DOCX, TXT, MD, CSV, SQLite, and DB files.
- SHA-256 hashing and duplicate detection by content hash.
- Source review states: `pending_review`, `accepted`, `maybe`, and `ignored`.
- Convenience YAML files for accepted, maybe, and ignored source IDs.
- JSONL command logs and YAML run summaries.
- Richer init wizard for research level, topic, research questions, subquestions, stakeholders, citation style, output type, data expectations, AI preference metadata, evidence/privacy preferences, and source review defaults.
- Workspace discovery when `--workspace` is omitted.
- Automatic selection when exactly one workspace is discovered.
- Numbered workspace selection and local default workspace memory when several workspaces exist.
- Concrete next-step command examples after successful init.
- MIT license and author metadata.
- README, TODO, architecture, and validation documentation for the Phase 1 baseline.

Notes:

- AI behavior is not implemented in Phase 1. Init records local AI preference metadata only and keeps AI disabled.
- Zotero support is storage-folder scanning only. Zotero API collection selection is planned for later work.
- Accepted sources are tracked locally, but downstream conversion and research workflows are planned for Phase 2 and later.
